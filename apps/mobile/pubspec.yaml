name: kwork_mobile
description: KWork Mob — мобильное приложение селлера (Flutter, §3 / §19)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.10.0
  shared_preferences: ^2.5.5
  go_router: ^17.3.0
  flutter_secure_storage: ^10.3.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
