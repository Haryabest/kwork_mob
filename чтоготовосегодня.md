# Что готово сегодня (2026-07-14)

## Утро / предыдущие срезы
- Seller settings 2FA + push prefs (web) + AuthCard 2FA
- NSFW SLA / escalate + photo preview + blacklist CRUD
- Mobile shoot-link deep link + campaign A/B stats
- Presigned URL → access_log; audit API key / tariff; disk/SMART dual-channel; GPU thermal alerts
- Миграции `023`–`024`

## Этот срез (5 задач)

### 1. Shoot-link usage stats (B2B §3.15.4)
- `shoot_links.company_stats` / `admin_overview`
- API: `GET /company/shoot_links/stats`, `GET /admin/companies/{id}/shoot-links`, `GET /admin/shoot-links/stats`
- Admin CompanyDetail + seller Team modal: created / expired / success

### 2. iOS Universal Links `/shoot/{token}`
- `Runner.entitlements` associated domains + `CODE_SIGN_ENTITLEMENTS`
- Info.plist: `FlutterDeepLinkingEnabled` + `kworkmob://` URL scheme
- `public/.well-known/apple-app-site-association` + `assetlinks.json`
- Web shoot page: custom scheme + Universal Link hint

### 3. Campaign templates → click tracker
- `click_track_url` / `inject_tracked_cta` в письма и push data
- CTA URL в конструкторе кампаний → `GET /api/v1/campaigns/{id}/click`

### 4. Restore sources → access_log + audit (§9.1.3 / §10.7.7)
- `POST /models/{uuid}/restore-sources` → presigned ZIP
- `access_log.action=restore_sources`, `audit_log.action=restore_from_cloud`
- Seller + mobile UI кнопки

### 5. Order cancel → audit_log
- `POST /orders/{id}/cancel` пишет `order_cancelled` в `audit_log`
# Что готово сегодня (2026-07-14)

## Утро / предыдущий срез
- Seller settings 2FA + push prefs (web `/settings`) + AuthCard 2FA challenge
- NSFW queue SLA / escalate (Celery + admin UI)
- Mobile shoot-link deep link `/shoot/{token}` + Android + web open-in-app
- Admin campaign A/B stats (funnel / by_variant / CTR / click track)
- Миграция `023_prefs_campaign_clicks`

## Этот срез (5 задач)

### 1. Presigned URL issue → access_log (§10.7.2 / §10.7.7)
- `access_log.log_access` + actions: `download`, `presign_get`, `presign_put`
- Логирование при: model preview/download, `/orders/photos/prepare`, `/storage/presign-upload|download`

### 2. Company audit critical events (§10.7.7 / §21.4.1)
- `api_key_created` / `api_key_revoked` → `audit_log`
- `tariff_price_changed` → `audit_log` при смене тарифа

### 3. Email dual-channel disk/SMART (§12.4)
- `alerts.send_dual` / `send_email_alert` (до 5 адресов)
- `storage_alerts` шлёт Telegram **и** email
- `email.send_alert_email` для системных алертов

### 4. Worker GPU thermal → Telegram (+email) via heartbeat (§12.4.1 / §13.4)
- `gpu_thermal.maybe_alert_from_metrics` при metrics и `overheating`
- Порог `GPU_TEMP_ALERT_C=85`, cooldown `GPU_TEMP_ALERT_COOLDOWN_SEC`

### 5. NSFW photo preview + blacklist CRUD (§10.8 / §11)
- Reports с `photo_previews` + `GET /admin/nsfw/{id}/photos`
- Таблица `moderation_blacklist`, CRUD API, admin UI модалка
- Проверка заказа через DB blacklist
- Миграция `024_moderation_blacklist`

---

## Срез: queue/all-busy/offline alerts + age-gate + soft KPI + invite audit (2026-07-14)

### 1. Queue length / all-busy dual alerts (§12.4.1)
- `ops_alerts.check_and_alert`: очередь > `QUEUE_ALERT_LENGTH` (20) → Telegram + email
- All workers busy > `ALL_BUSY_ALERT_MINUTES` (5) → dual-channel
- Celery beat `ops-alerts-every-minute` + `POST /admin/ops-alerts/check`

### 2. Age-gate admin viewer (§10.8.3 / §11)
- `GET /admin/age-verifications` — journal `age_verification` + verified users
- Moderation UI tab «Проверки возраста»

### 3. Soft-launch KPI charts
- `GET /admin/soft-launch/kpi` — funnel / orders / cancel / revenue / daily
- SoftLaunchPage: live Progress bars + target conv ≥60%

### 4. Worker offline 30s alert (§12.4.1)
- Hub + PG: no heartbeat ≥ `WORKER_OFFLINE_ALERT_SECONDS` (30) → Telegram + email
- Cooldown `WORKER_OFFLINE_ALERT_COOLDOWN_SEC`

### 5. Member invite/remove audit completeness (§10.7.7 / §21.4.1)
- Invite → `audit_log.company_invite_sent`
- Accept → `company_member_joined`
- Remove → `company_member_removed` (вместо `member.remove`)

---

## Срез: shoot TTL / click tracker / AASA / quality alerts (2026-07-14)

### 1. Shoot-link photo TTL 7d cleanup (§3.15.4)
- `shoot_cleanup.cleanup_stale_photos` — удаляет `photos/{task_uuid}/` через 7 дней, если генерация не запущена
- `photos_uploaded_at` на upload/complete; Celery daily + `POST /admin/shoot-links/cleanup-photos`
- `SHOOT_LINK_PHOTO_TTL_DAYS=7`

### 2. Referral / nth_free emails → click tracker
- `_activate_referral` / `_activate_nth_free` → `inject_tracked_cta` (как timed_discount)
- Referral CTA: `/register?ref={code}` через `/api/v1/campaigns/{id}/click`

### 3. AASA TEAMID + prod host (§3.15)
- Dynamic routes: `/.well-known/apple-app-site-association`, `/.well-known/assetlinks.json`
- Env: `APPLE_TEAM_ID`, `IOS_BUNDLE_ID`, `ANDROID_*`, host = `SELLER_PUBLIC_URL`
- Admin preview: `GET /admin/well-known/*`

### 4. Publication conversion &lt;30%/week alert (§12.4.1)
- `quality_alerts.check_publication_conversion` — email если downloaded→verified &lt; 30%
- Celery hourly + `POST /admin/quality-alerts/check`

### 5. Fallback segmentation &gt;15%/24h alert (§11.2.5 / §12.4.1)
- Таблица `segmentation_events` (миграция `025`)
- Worker шлёт `segmentation_stats` / device в payload; alert email по device/OS
- `orders.device_model` / `os_version` на create

---

## Срез: YooKassa / corporate / age CSV / mass shoot / ZIP SHA (2026-07-14)

### 1. YooKassa >5 consecutive errors (§12.4.1)
- Redis streak `yookassa:error_streak` на create/get/refund
- При ≥5 → Telegram + email; успех сбрасывает streak
- `YOOKASSA_ERROR_STREAK_ALERT=5`, `GET /admin/yookassa/error-streak`

### 2. Corporate alerts (§12.4.1)
- No photographer после удаления последнего → email
- Low balance <5000 → email (+ webhook `balance.low`); Celery hourly scan
- Suspicious >50 orders / 10 min → Telegram + email на create order

### 3. Age-gate CSV export (§11)
- `GET /admin/age-verifications/export` + кнопка CSV в Модерации

### 4. Mass shoot-link >100/h (§12.4.1)
- `shoot_link_limits.assert_can_create` → 429 + email + `settings.shoot_link_blocked_until`

### 5. Broken ZIP SHA-256 mismatch (§12.4.1)
- `integrity_alerts` email при client mismatch и `verify_object_sha256` fail

---

## Срез: Redlock / webhook streaks / mobile device / API daily / escalation dual (2026-07-14)

### 1. Redlock conflict email (§12.4.1)
- `redlock_alerts.notify_redlock_conflict` → Email + `task_conflicts`
- Триггеры: WS `task_conflict`, duplicate completion (WS/HTTP)
- Cooldown 10 мин на task+reason

### 2. Webhook failures сверка с ТЗ
- **YooKassa inbound** ≥5 подряд на order → Telegram+email + `audit_log.yookassa_webhook_manual_review` (§8.1 / §14.1)
- **Company ERP webhooks** остаются **3** подряд (§14.5.4) + dual-channel сервису и email Owner

### 3. Mobile device_model / os_version
- `ApiClient.createOrder` + `UploadCheckoutScreen` передают `Platform` model/OS
- Попадает в `orders` и task payload

### 4. API key daily limit exceeded (§12.4.1)
- Middleware: суточный счётчик Redis; 429 + email при исчерпании
- `API_KEY_DEFAULT_DAILY_LIMIT=100000`

### 5. Task escalation dual-channel
- `notify_escalation` явно `telegram=True, email=True`

---

## Срез: Alert thresholds UI / shoot unblock / invoice PDF / API daily_limit / UA / task_conflicts (2026-07-14)

### 1. Alert settings UI — пороги §12.4.1
- Migration `026`: `alert_settings.thresholds` JSONB + `company_api_keys.daily_limit`
- `alert_thresholds` service: defaults ↔ DB ↔ Redis `alerts:thresholds`
- `GET/PUT /admin/alerts/settings` возвращает/сохраняет thresholds + email channel
- Admin SettingsPage: Telegram/Email + NumberInput по всем порогам
- Ops/quality/corporate alerts читают пороги из Redis

### 2. Shoot-link auto-unblock
- `clear_block_if_expired` при проверке + Celery `clear_expired_shoot_blocks` каждые 5 мин
- Снимает `shoot_link_blocked_until` после истечения

### 3. Company invoice PDF polish §8
- `build_invoice_pdf`: шапка 3dvektor, блоки Исполнитель/Заказчик (ИНН компании), таблица услуг/апсейлов, НДС, подписи акта, footer
- `invoice_for_order` подставляет `Company.name`/`inn` для B2B заказов

### 4. Per-key `daily_limit` на create API key
- `ApiKeyCreate.daily_limit` → колонка + Redis `rl:apikey:daily_cfg:{prefix}`
- Seller UI: поле «Daily limit» + колонка в таблице

### 5. Web-seller device hint (UA)
- `device_hint_from_ua` + fallback в `POST /orders/create` из User-Agent
- web-seller `orders/new` шлёт `device_model` / `os_version` с клиента

### 6. Admin task_conflicts viewer
- `GET /admin/task-conflicts`
- Страница `/task-conflicts` + nav «Task conflicts»

---

## Срез: YooKassa IP auth / support ticket / email×5 / storage HA UI / no-photographer Owner (2026-07-14)

### 1. YooKassa webhook signature verify (hardening) §8.4.1
- `yookassa_webhook_auth`: официальный IP allowlist ЮKassa + `assert_payment_authentic` (id/shop после GET /payments)
- Env: `YOOKASSA_WEBHOOK_IP_CHECK`, `YOOKASSA_WEBHOOK_ALLOW_PRIVATE`, `YOOKASSA_WEBHOOK_IP_ALLOWLIST`
- `POST /webhooks/yookassa` отклоняет чужие IP (403)

### 2. Support ticket из `yookassa_webhook_manual_review`
- При fail streak ≥5: audit + `support_tickets.create_manual_review_ticket` (category=payment, идемпотентность 48ч)
- Видно в admin support panel

### 3. Email recipients — до 5 адресов §12.4.2
- `normalize_email_recipients` / TagsInput в Settings
- API: `email_recipients[]` + `email_to` CSV; лимит 5

### 4. Storage cluster alerts UI (disk / SMART / replication) §11.16.5
- `cluster_ha` в `/storage/smart` (`MINIO_HA_JSON` / SMART JSON)
- `storage_alerts`: free <10%, SMART, temp, SSD wear, MinIO repl Failed, PG lag, node offline
- StoragePage: блок репликации + кнопка Check disk/SMART/repl→alerts
- Пороги в Settings / Redis

### 5. Company no-photographer alert — Owner email
- Dual email владельцу сервиса + письмо Owner компании (`send_marketing_email`)

---

## Срез: alert_log CSV / write-activity / Force Resync+Patroni / soft-launch KPI CSV (2026-07-14)

### 1. Admin alert_log viewer + CSV §12.4.3
- `GET /admin/alerts/log` — фильтры event_type / channel / ok / даты + `company_id`/`worker_id` из payload
- `GET /admin/alerts/log/export` → CSV
- Admin страница `/alert-log`

### 2. Write-activity heartbeat alert (>10 мин)
- `write_activity`: last write из Order/Transaction/TaskQueue + MinIO HA
- При нагрузке (queued/processing) и stale ≥10 мин → Critical dual-channel
- Freeze indicator ≥5 мин; Celery каждые 2 мин; UI на StoragePage

### 3. Force Resync MinIO / Restart Patroni §11.16.4
- `POST /admin/storage/force-resync-minio`, `POST /admin/storage/restart-patroni-replication`
- Hook URL / script / Redis flag + `audit_log`
- Кнопки на StoragePage

### 4. Soft-launch KPI export CSV
- `GET /admin/soft-launch/kpi/export`
- Кнопка «KPI CSV» на SoftLaunchPage

---

## Срез: Owner notify prefs / refund.succeeded / FIO / Loki logs / soft-launch checklist (2026-07-14)

### 1. Owner notification prefs §3.19
- `company_notify`: routing `owner_only` / `owner_manager` / `all` для `generation_done`, `photographer_uploaded`, `source_expire`, `low_balance`
- Хранение в `company.settings.notification_routing`
- GET/PATCH `/company/settings` → `notification_routing`
- Seller UI: Team → Политики (Select на событие)
- Хуки: task complete, shoot upload, low_balance → `notify_company_event`

### 2. YooKassa `refund.succeeded` webhook §8 / §3711
- `parse_webhook` / `get_refund`: refund_id vs payment_id
- `POST /webhooks/yookassa`: IP → GET refund → idempotent `Transaction(external_id=refund:…)` → push пользователю

### 3. FIO disk test §11.16.4
- `POST /admin/storage/fio-test` (~10 сек)
- Hook `FIO_TEST_URL` / script / Redis `storage:cmd:fio_test` + audit
- Кнопка «Запустить FIO-тест» на StoragePage

### 4. Docker/Loki logs viewer §11.16.4
- `GET /admin/storage/docker-logs` (+ `/containers`)
- Backend: Loki LogQL → docker proxy → mock
- Env: `LOKI_URL`, `DOCKER_LOGS_PROXY_URL`, `DOCKER_LOG_CONTAINERS`
- Modal «Посмотреть логи» на StoragePage

### 5. Soft-launch checklist → backend
- Таблица `soft_launch_checklist` (Alembic `027`)
- `GET/PUT /admin/soft-launch/checklist`
- SoftLaunchPage синхронизирует чекбоксы с API (не только localStorage)

---

## Срез: NSFW SLA polish / age-gate UX / source-expire / maintenance (§23.7) (2026-07-14)

### 1. NSFW SLA dashboard polish §10.8
- `nsfw_sla_dashboard`: pending / overdue / urgent≤6ч / avg_hours_left / verified_24h / sla_met_rate
- `GET /admin/nsfw/sla` → расширенная сводка + queue preview
- Escalate → dual-channel (Telegram + Email)
- ModerationPage: Progress SLA, фильтры overdue/urgent, сортировка, метрики

### 2. Age-gate / date_of_birth UX §10.8.3
- `age_gate`: коды `age_gate_required` / `age_under_18` / `invalid_birth_date`; skip если уже verified
- Seller: Modal при выборе 18+, skip поля если `age_verified` из `/user/me`
- Mobile: Dialog age-gate, `birthDate` в ShootDraft → `createOrder`, session.ageVerified

### 3. Source-expire push scheduler §3.4.3 / §9.1.2
- `source_expire.notify_expiring_sources`: за 7 / 3 / 1 день до TTL (default 30д)
- Routing `source_expire` (company) / user prefs; Redis идемпотентность
- Celery `source-expire-daily` + `POST /admin/source-expire/notify`
- Env: `SOURCE_PHOTOS_TTL_DAYS`

### 4. Maintenance checklist UI §23.7
- Таблица `maintenance_checklist` (Alembic `028`)
- `GET/PUT /admin/maintenance/checklist`
- `POST /admin/maintenance/cleanup-logs`, `POST /admin/maintenance/backup-restore-test`
- Admin page `/maintenance`: SMART snapshot + чек-лист + действия

---

## Срез: Node timeline / webhooks dashboard / storage extend / forecast / trash (2026-07-14)

### 1. Node availability timeline §11.16.3
- Alembic `029`: `storage_node_events`
- `node_timeline.record_node_heartbeats` — Celery */5 мин из MINIO_HA
- `GET /admin/storage/node-timeline?days=7`
- StoragePage: uptime bar (online/offline сегменты)

### 2. B2B webhook delivery retries dashboard §14.5.4
- `delivery_dashboard`: pending / DLQ / delivered 24h / success rate
- Admin `/webhooks` + `GET /admin/webhooks/deliveries/dashboard`
- Seller Team → Webhooks: badges pending/DLQ/OK 24h

### 3. «Продлить хранение» исходников (×3) §9.1.2
- `source_expires_at`, `source_extend_count` на models
- `POST /models/{uuid}/extend-storage` (лимит 3, audit)
- Seller card + mobile «+30 дн.»

### 4. Disk fill forecast / wearout alerts §23.7
- `disk_usage_samples` + Celery sample */5
- `disk_forecast` → days_until_full, wearout SMART
- `storage_alerts`: алерт при ≤30д и wearout &lt;15%
- StoragePage forecast card

### 5. Trash / restore исходников из облака §3.3 / §9
- `trashed_at`, корзина 30д, `purge_model_trash` daily
- `GET /models/trash`, trash/restore-from-trash API
- Seller `/models/trash` + confirm «Удалить»
- `restore-sources` presigned ZIP

### Тесты
- `test_storage_models_ops_slice.py` — 4 passed

---

## Срез: Native AR / checkout §19.8 / калибровка §3.7 (2026-07-15)

### 1. Native ARKit / ARCore §3.1.1 / §19.6
- `ArSessionPlugin.kt` (ARCore): pose stream, bbox, **`showMarker`** + anchor
- `ArPlugin.swift` (ARKit): pose stream, **`showMarker`** + ARAnchor
- `guided_dome_screen`: alignment через `NativeArBridge.isAligned` (не только gyro)
- MethodChannel `com.kwork.mob/ar` + `ar_events`

### 2. Оплата заказа физлица §19.8.1 / §8
- Flow: upload 12 фото → **`OrderCheckoutScreen`** (тариф, апсейлы, промо, ФИО)
- `POST /orders/create` → `awaiting_payment` → **`POST /orders/{id}/pay`** (redirect / sbp_qr)
- Backend: `OrderPayRequest`, `GET /orders/tariffs`, YooKassa + confirmation_data для СБП
- Mobile: `listUpsells`, `listTariffs`, `payOrder`, QR dialog как на balance

### 3. Калибровка масштаба §3.7
- `ScaleCalibrationService`: card / A4 / QR / manual, TTL 30 дней
- **`CalibrationWizardScreen`**: card, A4, QR+PDF link, ручной W×H×D
- Профиль → «Калибровка масштаба» + «Перекалибровать» / сброс
- Gate **real_scale** upsell без калибровки → wizard
- Мебель: saved calibration или ручной ввод

### Тесты
- `apps/mobile/test/scale_calibration_test.dart`

---

## Срез: resumable upload / models list / profile §3.4.1 / §19.4.3 / §19.14 (2026-07-15)

### 1. Resumable upload §3.4.1
- `UploadProgressService`: persist индексов 0..11, ZIP SHA, presigned session
- E2E ключ в SecureStorage
- `UploadCheckoutScreen`: «Продолжить загрузку», retry ×4, skip uploaded

### 2. Список моделей §19.4.3
- Thumbnail: local view_00 / `GET /models/{uuid}/thumbnail`
- Название, дата ДД.ММ.ГГГГ, статус публикации
- Меню «…»: GLB Ozon, USDZ WB, share, rate, pub link, regen, rename, trash
- Backend: `display_name`, `PATCH /models/{uuid}`, migration `030`

### 3. Профиль §19.14
- Смена пароля → `POST /auth/password/change`
- Удалить аккаунт → `POST /user/me/delete-request`
- Тёмная тема: system / light / dark (`AppThemeController`)

### Тесты
- `apps/mobile/test/upload_progress_test.dart`

---

## Срез: cloud restore / AR fusion / profile FIO / export format (2026-07-15)

### 1. Cloud backup restore §3.3.2
- Backend: `draft_backup.py`, `GET/POST /user/draft-backups*`, MinIO TTL 7д
- `CloudDraftBackupService`: sync partial ZIP, restore на splash
- Диалог «Восстановить черновики?» после переустановки

### 2. AR camera fusion (optional) §3.1.1
- Android: ARCore `Texture` + `capturePhoto` (единая AR-камера)
- iOS: `capturePhoto` через ARFrame
- `NativeArPreview`, Guided Dome без Flutter Camera при native AR

### 3. Profile FIO/INN §19.14.1
- `users.inn` encrypted (migration `031`)
- `PATCH /user/me` full_name + inn
- Поля в профиле + «Сохранить профиль»

### 4. Export format §19.14.3
- `ExportPrefsService` + server `export_format`
- Выбор .glb / .usdz в профиле, download использует preference

---

## Срез: pending upload banner / iOS AR Texture / auto-download GLB (2026-07-15)

### 1. Pending upload banner на Home §3.4.1
- `UploadProgressService.pendingSummary()` — uuid + X/12
- `_HomeTab`: баннер «Незавершённая загрузка» → `/home/shoot/upload`
- Обновление при возврате на вкладку Home

### 2. iOS AR Texture preview §3.1.1
- `ArCameraTexture` + `FlutterTextureRegistry` в `ArPlugin.swift`
- `startSession` возвращает `textureId` (как Android ARCore)
- Guided Dome: live `NativeArPreview` вместо текста «AR-камера активна»

### 3. Auto-download GLB §3.5.3
- `LocalModelLibrary.syncPendingDownloads()`: completed orders + models с `glb_url`
- Вызов: splash boot, HomeShell init, вкладка «Модели», WS completed (как раньше)

---

## Срез: profile phone / import GLB / mass extend §9.1.2 (2026-07-15)

### 1. Profile phone §19.14.1
- Mobile профиль: поле «Телефон», `session.phone`, `PATCH /user/me`
- Сохранение вместе с ФИО/ИНН (PII encrypted)

### 2. Import model §6.10
- Backend: `POST /models/import/prepare` (presigned PUT `imports/{uuid}/model.glb`)
- `POST /models/import`: Owner-only, `display_name`, `task_uuid` = model uuid, audit
- Mobile: `ImportModelScreen` + `file_picker`, профиль Owner → «Импорт модели»
- Web-seller: `/models/import` + кнопка «Импорт GLB» на списке моделей

### 3. Owner mass extend storage §9.1.2
- Mobile Team (Owner): «Продлить все исходники»
- Web-seller: кнопка уже была, добавлен import рядом

### Тесты
- `test_model_import_slice.py`

---

## Срез: corporate sync / import worker / 2FA UI (2026-07-15)

### 1. Corporate orders в syncPendingDownloads §3.5.3
- `GET /orders?company_id=` и `GET /user/models?company_id=` — заказы/модели компании
- Mobile: `syncPendingDownloads(companyId)` при корпоративном режиме
- Splash, Home, Models tab передают `session.companyId`

### 2. Import validation worker §6.10 (async)
- `import_validation.enqueue_import_validation` → очередь high priority
- `validate_import_glb.py`: GLB 2.0, PBR stubs, Draco если >15 МБ
- Worker `import_validate` pipeline; `publish_status`: validating → imported / failed
- Mobile/web: redirect на очередь при `import_validating`

### 3. Profile 2FA UI §19.14.4
- Карточка безопасности: QR (`otpauth_uri`), копирование секрета, пошаговый wizard
- Owner required banner §10.7.5

---

## Срез: thumbnail / import pricing / import badges (2026-07-15)

### 1. Worker thumbnail.jpg 512×512 §9.2.1 / §19.4.3
- `worker/scripts/generate_thumbnail.py` — из `view_00` или текстуры GLB → `final/thumbnail.jpg`
- Шаг в `PIPELINE_STEPS` и `IMPORT_PIPELINE`; upload `{uuid}/final/thumbnail.jpg` в MinIO

### 2. Import pricing §6.10
- Тариф `import_glb` (default 500 ₽, admin может выставить 0)
- `GET /models/import/price`, `import_price_rub` в prepare/import
- Списание `charge_company` при импорте (402 при нехватке баланса)
- Admin Settings: поле «Импорт GLB, ₽»
- Mobile + web-seller: показ цены на экране импорта

### 3. Models list badges (mobile)
- `ModelsScreen`: иконки + `FBadge` для `import_validating` / `imported` / `import_failed`
- Человекочитаемые подписи «Проверка импорта», «Импортировано», «Ошибка импорта»

### Тесты
- `test_generate_thumbnail_512` в `test_model_import_slice.py`

---

## Срез: seller badges / import NSFW / orders filter / Ops E2E (2026-07-15)

### 1. Web-seller models list badges
- `PUBLISH_LABEL` + цвета для `import_validating` / `imported` / `import_failed`
- Карточка модели `[uuid]/page.tsx` — те же статусы

### 2. Import NSFW scan §6.10 / §10.8
- Worker `scan_import_nsfw.py` — текстуры GLB, heuristic/NudeNet (как §10.8)
- Шаг в `IMPORT_PIPELINE`; skip для `category=adult`
- `mark_failed`: `import_nsfw_detected` → `block_order` + refund (без двойного возврата)

### 3. Company orders — фильтр исполнитель §3.16.2
- `GET /orders?company_id=&user_id=` — Owner/Manager видят все, фильтр по сотруднику
- `GET /company/members` — доступ Owner/Manager (`require_manager`)
- Web-seller `/orders`: Select «Исполнитель», колонка автора

### 4. Ops E2E smoke
- `worker/scripts/e2e_import_acceptance.py` — локальный import pipeline + thumbnail
- `worker/scripts/e2e_import_api.ps1` — API import → poll completed → thumbnail
- `worker/scripts/e2e_cloud_restore.ps1` — restore-sources presigned ZIP
- `docs/deployment/E2E_SMOKE_CHECKS.md` — iOS AR Texture checklist §3.1.1

### Тесты
- `test_scan_import_nsfw_skip_adult` + import/thumbnail в `test_model_import_slice.py`

---

## Срез: mobile orders filter / NSFW UI / mass import / tx filter (2026-07-15)

### 1. Mobile orders — фильтр исполнитель §3.16.2
- `listOrders(companyId, userId)`; `_OrdersTab` + `canFilterCompanyOrders`
- Select сотрудника, подписи статусов включая `blocked_nsfw`

### 2. Import NSFW UI — blocked_nsfw
- `GET /user/models` → `order_status`
- Mobile + web-seller models list: badge «NSFW блок» при `order_status=blocked_nsfw`
- Mobile orders: иконка + label «NSFW блок»

### 3. Mass import API §6.10
- `POST /models/import/bulk/prepare` — до 100 presigned PUT
- `POST /models/import/bulk` — пакетный импорт, общая проверка баланса
- Сервис `import_models.queue_single_import`

### 4. Company transactions — фильтр сотрудник §8
- `GET /company/transactions?user_id=` (Owner/Manager)
- Web-seller `/balance`: корпоративный режим + фильтр сотрудника

---

## Срез: mass import UI / mobile balance / NSFW banner / API key (2026-07-15)

### 1. Mass import UI web-seller §6.10
- `/models/import/bulk` — 11–100 GLB, bulk prepare + upload + import
- Кнопки на списке моделей и странице одиночного импорта

### 2. Mobile company balance §8
- `BalanceScreen`: корпоративный режим, `/company/transactions`, фильтр сотрудника
- Баланс компании из `/company/mine`

### 3. Queue detail — blocked_nsfw banner
- Mobile `QueueScreen`: alert §10.8, отключена отмена
- Web-seller `/orders/[id]`: Alert NSFW блок

### 4. Mass import API key §8.8
- Scope `import:create` в ALLOWED_SCOPES
- `POST /models/import/bulk/prepare|bulk` — JWT Owner или X-API-Key
- `import_models.resolve_import_actor`

---

## Срез: CSV export / company topup / push prefs NSFW (2026-07-15)

### 1. Export transactions CSV §8 / §20.3.4
- `GET /company/transactions/export?user_id=&from=&to=&type=` — Owner, CSV
- `company_balance.export_company_transactions_csv` + фильтр сотрудника
- Web-seller `/balance`: экспорт через backend для Owner

### 2. Company balance topup web-seller §20.3 / §19.14.2
- Кнопка «Пополнить баланс компании» для Owner
- `POST /company/balance/topup`, return_url `/balance`

### 3. Push prefs NSFW / refund §3.4.3 / §20.8.3
- Mobile: toggles `refund` + `nsfw_blocked` в профиле
- Backend: `notification_prefs.nsfw_blocked`, push при NSFW block
- `user_wants_notification` — refund webhook уважает pref

---

## Срез: balance filters / low balance / SBP QR / push master / inbox (2026-07-15)

### 1. Balance date filters §20.3.4
- Web-seller `/balance`: фильтры `from`/`to`/`type` + сотрудник
- Backend: `GET /user/transactions` и `/company/transactions` с теми же query

### 2. Company low balance notify §20.3.5
- Web-seller `/balance`: порог `low_balance_threshold` для Owner
- `PATCH /company/settings` — webhook balance.low

### 3. SBP QR topup §20.3.3
- Web-seller модалка: карта + «Сгенерировать QR СБП»
- `qrcode.react` + `confirmation_data` от ЮKassa

### 4. Push master toggles §19.14.3
- Mobile профиль: `push_enabled` / `email_enabled`
- События отключаются если оба канала выкл

### 5. Notifications inbox §19.16
- Иконки по типу (модель / refund / NSFW)
- `blocked_nsfw` + refund из sync orders
- «Очистить все» + mark read

---

## Срез: pagination / SBP poll / marketing / inbox API / mobile filters (2026-07-15)

### 1. Balance pagination §20.3.4
- `limit`/`offset`/`total` на `/user/transactions` и `/company/transactions`
- Web-seller `/balance`: Select 20/50/100 + Pagination

### 2. Payment status poll §20.3.3
- `GET /user/balance/payment/{id}` и `/company/balance/payment/{id}`
- Web QR modal: poll каждые 3 сек до succeeded/canceled
- Mobile SBP: Timer poll после QR

### 3. Marketing opt-in §20.8.3
- Web-seller `/settings`: описание отписки от маркетинга + `nsfw_blocked` в prefs

### 4. Notifications server sync §19.16
- Таблица `user_notifications`, migration 032
- `GET/DELETE /user/notifications`, read-all, mark read
- Push `send_to_user` → inbox record; mobile sync с сервера

### 5. Balance filters mobile §20.3.4
- `BalanceScreen`: from/to/type, page size, pagination
- `listTransactionsPage` / `listCompanyTransactionsPage`

---

## Срез: tx status / company topup mobile / web inbox / date picker / push deeplink (2026-07-15)

### 1. Transaction status column §20.3.4
- `transaction_status()` + `status`/`status_label` в API user/company transactions
- Web-seller `/balance`: колонка «Статус» + CSV export
- Mobile `BalanceScreen`: статус в списке транзакций

### 2. Company topup mobile §19.14.2
- Owner в корпоративном режиме: карта + СБП QR через `POST /company/balance/topup`
- `topupCompanyBalance()` + poll `/company/balance/payment/{id}`

### 3. Notifications web inbox §19.16
- `/notifications` — список, read-all, clear, переход в заказ/модель
- `SellerShell`: колокольчик с badge unread

### 4. Date picker mobile §20.3.4
- `BalanceScreen`: native `showDatePicker` для from/to вместо ручного ввода

### 5. Push deeplink inbox §19.16
- `deep_link_routes`: `kworkmob://open/notifications`
- `push_service._routeFromData`: NSFW/refund/generation → queue/model/notifications
- Background/cold start — сразу на целевой экран

---

## Срез: pending tx / web bell drawer / company topup screen / support deeplink / CSV mobile (2026-07-15)

### 1. Pending YooKassa tx status §20.3.4
- Таблица `balance_pending_payments`, migration 033
- Запись при создании topup (user/company), merge в `/transactions`
- Webhook `payment.canceled` + poll → статус «В обработке» до webhook

### 2. Web notifications mobile drawer
- `SellerShell`: пункт «Уведомления» с badge в navbar drawer (mobile)
- Header bell — desktop (`visibleFrom="sm"`)

### 3. Company topup mobile screen §19.14.2
- `/home/company-topup` — отдельный экран Owner (карта + СБП)
- Профиль: пункт «Баланс компании»; BalanceScreen — ссылка, без inline topup

### 4. Push deeplink support §19.16
- `kworkmob://open/support/ticket/{id}` + route `/home/support/ticket/:id`
- Admin reply → push с `ticket_id` и deeplink
- `FaqSupportScreen` открывает тред по `initialTicketId`

### 5. Balance CSV export mobile §20.3.4
- Owner: `GET /company/transactions/export` + `share_plus` share sheet
- Кнопка «Экспорт CSV» на `BalanceScreen` с учётом фильтров

---

## Срез: personal CSV / pending purge / web pending row / support tab / low balance banner (2026-07-15)

### 1. Personal tx CSV export mobile §20.3.4
- `GET /user/transactions/export` + pending rows в CSV
- Mobile: `exportUserTransactionsCsv()` + share sheet для личного баланса

### 2. Pending payment cleanup job
- Celery `purge_old_pending_payments` daily 04:20 — settled > 30d

### 3. Web balance pending row highlight
- `/balance`: оранжевый фон + filled badge для `pending` / «В обработке»

### 4. Support tab deeplink
- Push/deeplink → `/home?tab=support&supportTicket={id}`
- `HomeShell`: переключение bottom nav на Support + открытие треда

### 5. Company topup low balance banner §20.3.5
- `LowBalanceBanner` на `BalanceScreen` и `CompanyTopupScreen`
- Порог из `GET /company/settings` для Owner

---

## Срез: web CSV pending / failed webhook / mobile settings / foreground support / pending tx UI (2026-07-15)

### 1. Web balance export pending §20.3.4
- `/balance` export: server-side `/user/transactions/export` и `/company/transactions/export` (с pending)

### 2. Pending payment failed webhook
- `payment.failed` → status failed; `payment.waiting_for_capture` → upsert pending

### 3. Company settings mobile §20.3.5
- Owner: редактирование `low_balance_threshold` на `BalanceScreen` + `PATCH /company/settings`

### 4. Support push foreground tab
- `PushService.bindForegroundNavigate` — switch tab Support без `router.go` reload

### 5. Balance pending row mobile §20.3.4
- `_TxTile`: оранжевая подсветка + bold для «В обработке»

---

## Срез: waiting capture poll / web CSV Manager / failed push / policies / i18n kk·zh (2026-07-15)

### 1. Waiting capture poll job §8
- `refresh_stale_waiting_capture()` — YooKassa re-check pending > 5 min
- `settle_succeeded_topup()` — idempotent зачисление при succeeded
- Celery `poll_waiting_capture_pending` каждые 5 мин

### 2. Web balance export all roles §8
- `GET /company/transactions/export` — `can_view_finance` (Manager)
- Web `/balance`: `canFinance` вместо только Owner

### 3. Failed topup push notify §8
- `payment.failed` → `notify_topup_failed()` push Owner

### 4. Company policies screen mobile §19.14.2
- `CompanyPoliciesScreen` — политики + notification routing
- Route `/home/company-policies`, ссылка в профиле Owner

### 5. i18n kk/zh §19.14.3
- `app_kk.arb`, `app_zh.arb` (~100 ключей)
- `AppLocaleController`: ru/en/kk/zh
- L10n: onboarding, profile, balance, topup, policies, low balance banner

### Тесты
- `test_pending_payments_slice.py` — 7 passed

---

## Срез: Manager CSV / poll metrics / topup inbox / auth·shoot i18n (2026-07-15)

### 1. Mobile balance CSV Manager §20.3.4
- Export CSV для Manager с `can_view_finance` (как web)
- L10n share + snackbar «CSV экспортирован»

### 2. Pending poll metrics §8
- Prometheus `kwork_pending_payment_poll_total{outcome}`
- `record_pending_poll()` в Celery `poll_waiting_capture_pending`

### 3. Topup failed inbox + deeplink §19.16
- `notify_topup_failed`: dedup_key + deeplink `/home/balance`
- `PushService`: маршрут `topup_failed` → `/home/balance`
- Inbox через `send_to_user(record_inbox=True)`

### 4. Auth screen i18n §19.14.3
- Все шаги auth: login/register/2FA/legal — ru/en/kk/zh

### 5. Shoot flow i18n §19.14.3
- `CategoryScreen`, `QualityReviewScreen`, `GuidedDomeScreen` — полный l10n

### Тесты
- `test_pending_payments_slice.py` — 8 passed

---

## Срез: topup email / Grafana alert / filters persist / upload·guest i18n (2026-07-15)

### 1. Topup failed email template §3.4.3
- `send_topup_failed_email()` — HTML + кнопка «Открыть баланс»
- Push fallback: `SELLER_PUBLIC_URL/balance` deeplink

### 2. Pending poll Grafana alert §11
- Panel в `kwork-ops.json`
- `infra/grafana/provisioning/alerting/pending_poll.yaml` — failed > 3 за 15 мин
- Prometheus datasource `uid: prometheus`

### 3. Manager balance filters persist §20.3.4
- Web: `localStorage` (`balanceFilters.ts`)
- Mobile: `BalanceFiltersPrefs` (SharedPreferences)

### 4. Upload/checkout i18n §19.14.3
- `UploadCheckoutScreen`, `OrderCheckoutScreen` — ru/en/kk/zh

### 5. Guest shoot i18n §19.14.3
- `GuestShootGateScreen`, `GuestShootUploadScreen` — ru/en/kk/zh

### Тесты
- `test_topup_failed_email_slice.py` — 2 passed
- `test_pending_payments_slice.py` — 10 passed

---

## Срез: topup_failed pref / Grafana contact / balance filters API / home i18n / notifications i18n (2026-07-15)

### 1. Push prefs topup_failed §3.4.3
- `notification_prefs.topup_failed` — allowed в PATCH `/user/me`
- `notify_topup_failed()` — `user_wants_notification(owner, "topup_failed")`
- Mobile profile toggle + `PushService` defaults
- Web settings `PREF_LABELS.topup_failed`

### 2. Grafana contact point §11
- `infra/grafana/provisioning/alerting/contactpoints.yaml` — Telegram + Slack (`$__env{...}`)
- `policies.yaml` — route `team=ops` → telegram + slack
- `docker-compose.ha.yml` — mount alerting + env vars

### 3. Balance filters API §20.3.4 / §8
- `GET/PUT /user/balance-filters`, `GET/PUT /company/balance-filters`
- `balance_filters.py` — saved views в `notification_prefs.balance_tx_filters`
- Web `loadBalanceFiltersSynced` / mobile `BalanceFiltersPrefs.loadSynced`

### 4. Home/orders i18n §19.14.3
- `_HomeTab`, `_OrdersTab`, switch mode — ru/en/kk/zh ARB keys

### 5. Notifications i18n §19.16
- `NotificationsScreen` title + empty state — ru/en/kk/zh

### Тесты
- `test_balance_filters_slice.py` — 4 passed
- `test_pending_payments_slice.py` + `test_topup_failed_email_slice.py` — 14 passed total slice

---

# Что готово (2026-07-15) — 5 эпиков

## Эпик A — Notification pipeline (§3.4.3)
- **A7 master toggles в `send_to_user`**: учёт `push_enabled` / `email_enabled` (`respect_prefs`), push и email-fallback пропускаются при выключенных переключателях.
- **A3 отложенный email-fallback 5 мин**: если push доставлен, но уведомление не открыто за 5 мин — дублируется на email.
  - `services/push_fallback.py` (Redis ZSET `push:fallback:due`, `schedule` / `process_due`).
  - Celery beat `run_push_email_fallback` (`push-email-fallback-every-minute`).
  - `email.send_notification_email` (транзакционное письмо без marketing-footer).
- **A2 `support_reply` pref**: добавлен в allowed prefs (`user.py`), в mobile (`home_shell`, `push_service`), web-seller `settings`.
- **A4 inbox-иконки**: `topup_failed` (creditCard) + `support_reply` (messageCircle) в `notifications_screen.dart`.
- Тесты: `test_push_fallback_slice.py` (3) — passed.

## Эпик E — Admin B2B Command Center (§11.4 / §11.6 / §11.9)
Backend (`api/v1/admin.py`):
- `GET /admin/invitations` (+фильтр status) и `GET /admin/companies/{id}/invitations`
- `POST /admin/invitations/{id}/revoke` → audit `invitation_revoked`
- `GET /admin/companies/{id}/api-keys` + `POST .../api-keys/{key_id}/revoke` → audit `api_key_revoked`
- `PATCH /admin/companies/{id}/members/{user_id}/limits` (max_concurrent_orders, monthly_spending_limit) → audit
- `GET/PUT /admin/companies/{id}/price-overrides` — индивидуальные цены B2B (fixed / percent)
- `GET /admin/support/stats` — открытые/отвеченные тикеты, avg первого ответа, нагрузка агентов
- **Индивидуальные цены применяются в заказах**: `tariffs.get_amount_for_company` + `apply_company_override`, wired в `orders.py` и bulk `company.py`.

Frontend (`web-admin`):
- `InvitationsPage` — реальный API + фильтр статуса + revoke
- `SupportStatsPage` — метрики SLA + нагрузка агентов
- `CompanyDetailPage` — лимиты сотрудников (modal), API-ключи (revoke), индивидуальные цены (fixed/percent), приглашения, аудит
- Тесты: `test_company_price_override_slice.py` (4) — passed.

## Эпик C — Mobile i18n (§16 / §19)
Полностью локализованы (ru/en/kk/zh) 3 ключевых экрана:
- `queue_screen.dart` — 15 строк (статусы через `orderStatus*`, WS-состояния, отмена, NSFW-блок)
- `faq_support_screen.dart` — FAQ/тикеты/тред (~20 строк)
- `team_screen.dart` — участники/приглашение/аудит, лимиты, продление исходников (~22 строки)
- ~55 новых ключей ×4 языка в `app_ru/en/kk/zh.arb`, `flutter gen-l10n` OK, `flutter analyze` — без ошибок.

## Эпик E-platform — Admin UX foundation (§11.2 / §11.15)
- **recharts**: линейный график поступления заказов (48ч) + столбчатый рейтингов на `DashboardPage`.
- **Тёмная тема**: toggle в `AppLayout` (`useMantineColorScheme`), `theme.ts` через `light-dark()`, dark CSS-переменные в `vektor.css`.
- **React Query**: `QueryClientProvider` + `QueryClient` (staleTime 15с) в `main.tsx`.
- **Build health**: устранены все ошибки `tsc --noEmit` (unused imports, типизация `theme.ts`), `npm run build` проходит.

## Эпик F2 + H — Ops / observability (§12)
- Celery beat `push-email-fallback-every-minute` (см. Эпик A); остальные периодические задачи уже присутствовали.
- **Grafana**: docker-compose монтирует `./infra/grafana/dashboards` → JSON-дашборды теперь загружаются; env-параметры `GF_SECURITY_ADMIN_USER/PASSWORD`, `GF_SERVER_ROOT_URL`, `GF_USERS_ALLOW_SIGN_UP`, `GF_INSTALL_PLUGINS`, `CLICKHOUSE_PASSWORD`; ClickHouse datasource password через `$CLICKHOUSE_PASSWORD`.
- `.env.example` дополнен блоком Grafana.

### Тесты (срез)
- backend: `test_push_fallback_slice`, `test_company_price_override_slice`, `test_topup_failed_email_slice`, `test_notification_inbox_slice` — passed; полный unit-прогон 95 passed (1 fail — `test_auth` требует живой Redis/PG, не связан с изменениями).
- web-admin: `tsc --noEmit` — 0 ошибок.
- mobile: `flutter analyze` изменённых экранов — без ошибок.

---

# Итерация 2 (эпики G → K)

## Эпик G — Mobile i18n: model_viewer (§16 / §19)
- `model_viewer_screen.dart` (1229 строк) локализован **полностью**: список моделей + 3D-viewer + меню + рейтинг + публикация WB/Ozon + все диалоги/снекбары.
- ~70 строк UI → `AppLocalizations`; причины оценки переведены на стабильные коды (`blurry/holes/scale/color/other`) с локализованными подписями (не ломает API).
- ~70 новых ключей `mv*` ×4 языка (ru/en/kk/zh); переиспользованы `cancel/save/yes/faqClose/faqSend/queueStatus/orderStatusBlockedNsfw`.
- Исправлена pre-existing ошибка компиляции `FBadgeStyle.primary()` → `FBadge(child:...)`.
- `flutter gen-l10n` OK, `flutter analyze model_viewer_screen.dart` — 0 ошибок (остались только info-lints, общие для проекта).
- Остальные экраны из списка G (calibration, storage_settings, import_model, balance, trash, shoot_link, consent_gate, guided_dome, guest_shoot) — follow-up.

## Эпик H — QA-инфраструктура и CI (§1.4 / Phase P)
- `tests/conftest.py`: фикстуры `_schema` (create_all тестовой БД + dispose пула), `client`, `db`, `redis_client` (rebind singleton к loop, flush до/после), `unique_email`; маркер `integration` со **skip при недоступности** сервисов.
- `tests/integration/`: `test_redlock_concurrency.py` (SET NX EX — один воркер захватывает лок, TTL) + `test_auth_flow.py` (register→verify→login→refresh→logout, отказ по неверному паролю). Дубликат `test_auth.py` удалён.
- web-admin/web-seller: `vitest.config.ts` + `src/test/setup.ts` + скрипты `typecheck/test`; devDeps vitest + Testing Library + jsdom. Тесты: `api.test.ts` (getApiError), `balanceFilters.test.ts` (localStorage + серверный sync).
- `.github/workflows/ci.yml`: jobs **backend** (postgres+redis services, ruff, openapi --check, pytest), **web-admin** (tsc+vitest), **web-seller** (tsc+vitest), **mobile** (gen-l10n + analyze).
- Прогон: 95 unit passed, 4 integration skipped (нет локальных PG/Redis) — корректно.

## Эпик I — Web-seller UX (§20)
- `/orders`: **живые статусы** — тихий поллинг каждые 15с пока есть незавершённые заказы (visibility-aware), бейдж «Обновляется автоматически» + время обновления.
- `/team`: **QR-код** shoot-link (`qrcode.react`) в модалке съёмки рядом со ссылкой.
- Backend `auth.py`: `POST /auth/2fa/disable` (запрет для owner), `GET /auth/sessions`, `DELETE /auth/sessions/{id}`, `POST /auth/sessions/revoke-others` (по текущему refresh jti).
- `/settings`: **отключение 2FA** (по TOTP-коду) + **управление своими сессиями** (список, завершить одну / все кроме текущей).
- ruff — clean; routes зарегистрированы. Server-side пагинация `/models` и team search/role-фильтры — follow-up.

## Эпик J — Worker pipeline correctness (§5 / §6)
- **Баг-фикс**: `WATERMARK_SECRET` использовался, но не был определён в `worker_agent.py` (NameError на watermark-шаге) → добавлено `WATERMARK_SECRET = os.getenv("WATERMARK_HMAC_SECRET", ...)`.
- `validate_glb.py`: quality-gate больше не только по размеру/magic — реальный парсинг геометрии через trimesh (faces/vertices, `empty_geometry` = жёсткий провал), новая компонента `geometry` в весах.
- `apply_real_scale.py`: теперь **физически масштабирует меш** под калиброванный bbox (X/Y/Z) через trimesh + graceful fallback на metadata-only; extras `real_scale.mesh_rescaled`.
- py_compile + ruff — clean. Redlock (SET NX EX на Sentinel-master) корректен для single-master; multi-node Redlock не требуется.

## Эпик K — Документация в production (§15)
- `scripts/export_openapi.py`: экспорт OpenAPI в `docs/api/openapi.json` (+`.yaml`), режим `--check` для CI. Сгенерировано **270 путей**, версионируется в git.
- CI шаг «OpenAPI spec up to date».
- `docs/deployment/PRODUCTION_RUNBOOK.md`: единый **50-шаговый** runbook (подготовка → инфра → бэкенд → воркеры → клиенты → observability → приёмка → эксплуатация + rollback).
- `docs/support/PLAYBOOK.md`: playbook поддержки (SLA, 6 типовых сценариев, эскалация L1→L3, чек-лист).
- `docs/api/README.md` дополнен разделом про версионированную спеку и генерацию клиентов.

## Mobile prod readiness (~92%) — mobileready.md
- **i18n §16**: calibration, storage, import, balance, consent, shoot_link, guided_dome, company_topup, upload_checkout, category, checkout, router dialogs; `catalog_l10n.dart`, `guided_dome_l10n.dart`, `locale_format.dart`, WS/payment status l10n.
- **§19**: промокод validate+UI на checkout; баннеры кампаний (`GET /user/campaign_banners` + `CampaignBanner` на home); analytics flush (`POST /user/analytics/events`).
- **Fix**: `balance_screen` FTileGroup→Column; `flutter analyze` 0 errors; тесты `catalog_l10n_test`, `notification_text_test`.
- **Осталось**: prod Firebase/APNs, App Links native, signing/store, real-device QA, integration e2e.

---

# Итерация 3 — Mobile P1 (§19 / §20.3.4 / §20.8)

## Эпик L — Models filters UI (§19.9)
- `model_viewer_screen.dart`: поиск по названию/uuid, фильтры дата (from/to), тариф (small/large), автор (corp + `canFilterCompanyOrders`), клиентская фильтрация поверх списка.
- Backend: `GET /user/models` — поле `user_id` в items для corp-фильтра автора.

## Эпик M — Analytics track (§19.20)
- `AnalyticsService.track()`: `screen_view` при смене табов home shell + при старте; `shoot_complete` в `guided_dome_screen` перед review; `checkout_pay` после успешного `createOrder`.

## Эпик N — Checkout offline guard (§19.8)
- `api.dart`: `isNetworkError`, `userFacingError` → `errorNetwork` из l10n.
- `order_checkout_screen.dart`: `_boot` / `_submit` показывают «Нет интернета» вместо DioException.

## Эпик O — Balance named presets (§20.3.4)
- Backend: `GET/POST/DELETE /user/balance-filter-presets`, `/company/balance-filter-presets` (до 10 пресетов); сервис `list_presets/upsert_preset/delete_preset`.
- Mobile: UI в `balance_screen.dart` — список пресетов, применить, сохранить текущие фильтры, удалить.
- Тест: `test_balance_filter_presets_crud` в `test_balance_filters_slice.py`.

## Эпик P — Profile sessions mobile (§19.14.4 / §20.8)
- `api.dart`: `listAuthSessions`, `revokeAuthSession`, `revokeOtherAuthSessions` (+ `refreshToken` getter).
- `_ProfileTab` в `home_shell.dart`: список сессий, завершить одну / все кроме текущей.

## i18n
- ~16 новых ключей ×4 языка: `mvSearchHint`, `balancePresets*`, `profileSessions*`.

### Тесты
- `flutter gen-l10n` OK; `flutter analyze` изменённых файлов — 0 errors.
- backend preset unit-test добавлен (pytest env локально без venv — skip).

---

# Итерация 4 — Mobile P2 (§19.9 / §19.20 / §16 / §20.3.4)

## Эпик Q — Models server-side filters + pagination §19.9
- `GET /user/models`: query `search`, `date_from/to`, `tier`, `author_id`, `category`, `publish_filter`, `sort`, `limit`, `offset`; ответ `{items, total, limit, offset}`.
- Mobile: `listModelsPage()` в `api.dart`; `ModelsScreen` — серверные фильтры, debounce поиска, «Загрузить ещё».

## Эпик R — Analytics validation §19.20
- `app/schemas/analytics.py`: `AnalyticsEventItem` + `AnalyticsEventsBody` — whitelist событий, обязательные `props`, ISO `ts`.
- `POST /user/analytics/events` — 422 на невалидный batch.
- Тест `test_analytics_events_slice.py`.

## Эпик S — Analytics full coverage (major screens) §19.20
- `screen_view` track в `calibration_wizard_screen`, `storage_settings_screen`, `import_model_screen`, `trash_screen`, `queue_screen`.

## Эпик T — Model viewer API upload i18n §16
- `mvApiUploadTitle`, `mvApiSkuLabel`, `mvApiUploadBtn`, `mvLoadMore` ×4 языка; убраны `const Text` в upload-диалоге.

## Эпик U — Balance re-sync on corp switch §20.3.4
- `BalanceScreen`: `didUpdateWidget` + `session.addListener` → `_checkModeSwitch()` → сброс пресетов и `_restoreFilters()`.

### Тесты
- `test_models_filters_slice.py` — сигнатура query params.
- `flutter gen-l10n` + `flutter analyze` — 0 errors.

---

# Итерация 5 — Web-seller parity + analytics + trash (§20 / §19.20 / §3.3.1 / §15)

## Эпик V — Web-seller `/models` filters parity §20
- `apps/web-seller/src/app/models/page.tsx`: серверные query params (`search`, `date_from/to`, `tier`, `category`, `publish_filter`, `author_id`, `sort`, `limit`, `offset`); debounce поиска; Mantine `Pagination`; corp `company_id` + фильтр авторов для manager.

## Эпик W — OpenAPI regen §15
- `python scripts/export_openapi.py` → `docs/api/openapi.json` + `.yaml` (276 путей); новые params `/user/models`, `/models/trash`.

## Эпик X — Analytics track (ещё 4 экрана) §19.20
- `screen_view`: `notifications`, `team`, `api_keys`, `publish_guide`.

## Эпик Y — Profile/security i18n + disable 2FA §19.14.4
- Ключи `profileDisable2fa*` ×4 языка; исправлен kk `profileSecretCopied`.
- Mobile: `twoFaDisable()` API + UI в профиле; snackbar ошибок через `formatApiError` (не `$e`).

## Эпик Z — Models trash server pagination §3.3.1
- Backend: `list_trash(limit, offset)` → `{items, total, limit, offset}`.
- Mobile `listTrashModelsPage` + «Загрузить ещё» в `trash_screen.dart`.
- Web-seller `models/trash/page.tsx` — пагинация.

### Тесты
- `test_trash_pagination_slice.py`; `flutter analyze` — 0 errors.

---

# Итерация 6 — Team filters + analytics persistence + i18n (§20 / §19.20 / §16 / §1.4)

## Эпик AA — Team server filters §20
- `GET /company/members`: query `search`, `role`, `limit`, `offset`; ответ `{items, total, limit, offset, company_id}`.
- Mobile `listCompanyMembers()` + UI фильтры в `team_screen.dart`.
- Web-seller `/team`: включены search/role FilterRow.

## Эпик AB — Analytics ingest persistence §19.20
- Модель `MobileAnalyticsEvent` + миграция `034_mobile_analytics_events`.
- `analytics_ingest.persist_events()` → PG + mirror ClickHouse `mobile_analytics_events`.
- `POST /user/analytics/events` пишет в БД.

## Эпик AC — Analytics track (ещё 6 экранов) §19.20
- `screen_view`: `balance`, `company_topup`, `company_policies`, `shoot_link`, `guest_shoot`, `consent_gate`.

## Эпик AD — Guest shoot + consent gate i18n §16
- Ключи `guestMissingFrame`, `consentDocVersion`, `teamSearchHint`, `teamRoleAll` ×4 языка.
- Исправлен kk `guestSentToOwner` (жіберілді).

## Эпик AE — Integration tests §1.4
- `test_models_trash_api_smoke.py` — models filters + trash pagination + analytics persist smoke.
- `test_team_members_filters_slice.py`, `test_analytics_ingest_slice.py`.
- `test_mobile_api_smoke.py` — валидный `screen_view` вместо `ci_smoke`.

### Тесты
- pytest slice 4 passed; `flutter gen-l10n` + analyze изменённых файлов — 0 errors.

---

# Итерация 7 — ClickHouse DDL + team pagination + wizard analytics + E2E guest (§19.20 / §20 / §3.15)

## Эпик AF — ClickHouse DDL mobile analytics §19.20
- `infra/clickhouse/init.sql`: таблица `mobile_analytics_events` + MV `mobile_analytics_daily`.
- Тест `test_clickhouse_analytics_ddl_slice.py`.

## Эпик AG — Team pagination UI §20
- Mobile `team_screen.dart`: `_memberPageSize=20`, «Загрузить ещё» (`mvLoadMore`).
- Web-seller `/team`: `MEMBER_PAGE=20`, кнопка «Загрузить ещё (n/total)».

## Эпик AH — Analytics track settings + shoot wizard §19.20
- `screen_view`: `settings` (`_ProfileTab`), `shoot_category`, `shoot_dome`, `shoot_quality`, `shoot_upload`, `order_checkout`.

## Эпик AI — E2E guest shoot staging §3.15
- `worker/scripts/e2e_guest_shoot_api.ps1`: create link → presign PUT ×12 → complete.
- `tests/integration/test_guest_shoot_api_smoke.py` (multipart upload, skip без MinIO).

## Эпик AJ — Ops checklists §19.21
- `docs/deployment/OPS_DEVICE_QA.md` — iOS AR, Firebase prod, real-device QA.
- `E2E_SMOKE_CHECKS.md` §4 guest shoot + §5 ops ссылки.

### Тесты
- `test_clickhouse_analytics_ddl_slice.py`; pytest slice OK.

---

# Итерация 8 — Grafana analytics + webhook + track + shoot_step (§19.20 / §3.15 / §16)

## Эпик AK — Grafana mobile analytics §19.20
- `kwork-clickhouse.json`: timeseries `mobile_analytics_daily` (14d) + table top events (7d).

## Эпик AL — Guest shoot webhook test §3.15
- `test_guest_shoot_webhook_smoke.py`: monkeypatch deliver → `shoot_link.uploaded` + deliveries API.
- `e2e_guest_shoot_webhook.ps1`: staging с `WEBHOOK_URL` + poll deliveries.

## Эпик AM — Analytics track support/models §19.20
- `screen_view`: `faq_support`, `models`, `model_viewer`.

## Эпик AN — Team load-more i18n §16
- Ключ `teamLoadMore` ×4 языка; `team_screen` вместо `mvLoadMore`.

## Эпик AO — shoot_step event §19.20
- Backend: `shoot_step` в `ALLOWED_EVENTS` (props `model_uuid`, `step` 1..12).
- `guided_dome_screen`: track при старте и после смены ракурса.

### Тесты
- `test_analytics_events_slice.py` shoot_step; pytest 8 passed; `flutter gen-l10n` OK.

---

# Итерация 9 — screen MV + DLQ replay + campaign/export track + shoot_step_retry (§19.20 / §14.5.4)

## Эпик AP — Grafana screen breakdown §19.20
- CH MV `mobile_analytics_screen_daily` (JSONExtractString props.screen).
- Grafana panel `screen_view breakdown (7d)` в `kwork-clickhouse.json`.

## Эпик AQ — Webhook DLQ replay test §14.5.4
- `test_webhook_replay_dlq_smoke.py`: DLQ delivery → `POST /webhooks/deliveries/replay-dlq` → delivered.

## Эпик AR — Analytics campaign + export prefs §19.20
- `screen_view`: `campaign_banner`, `campaign_banner_dismiss`, `export_prefs` в `home_shell.dart`.

## Эпик AS — shoot_step_retry §19.20
- Backend: `shoot_step_retry` (props `model_uuid`, `step`, `error_type`).
- `quality_review_screen._reshoot()` — track blurry/off_center/overexposed.

## Эпик AT — Models screen_view dedup §19.20
- Убран `screen_view models` из `ModelsScreen.initState` (home tab уже трекает).

### Тесты
- `test_analytics_events_slice.py` + `test_clickhouse_analytics_ddl_slice.py` — 9 passed.

---

# Итерация 10 — CH backfill + admin analytics export + track P8 (§19.20)

## Эпик AU — CH screen MV backfill §19.20
- `infra/clickhouse/backfill_mobile_analytics_screen_daily.sql` + `backfill_screen_mv.ps1`.

## Эпик AV — Admin analytics export API §19.20
- `analytics_query.screen_breakdown()` — CH MV + PG fallback.
- `GET /admin/analytics/screens` (+ `?format=csv`).

## Эпик AW — Analytics pending upload + mode switch §19.20
- `pending_upload_banner` при появлении баннера; `mode_personal` / `mode_corporate` после switch.

## Эпик AX — Campaign banner_id §19.20
- Mobile: `banner_id` в props для `campaign_banner` / `campaign_banner_dismiss`.
- Backend: validation `props.banner_id` int для `screen_view`.

## Эпик AY — shoot_step_retry missing_frame §19.20
- `quality_review_screen._analyze()` — auto-fail при отсутствии файла кадра.

### Тесты
- `test_analytics_query_slice.py`; pytest slice OK.

---

# Итерация 11 — PG→CH sync + admin events export + campaign CTR + track P9 (§19.20)

## Эпик AZ — Analytics PG→CH sync job §19.20
- Alembic `035`: `mobile_analytics_events.ch_synced_at`.
- `analytics_sync.sync_unsynced()` — зеркалирование несинхронизированных строк в CH.
- Ingest: при успешной записи в CH — `ch_synced_at` на PG row.
- Celery `sync_analytics_to_clickhouse` beat */15 мин.
- `POST /admin/analytics/sync` — ручной триггер.

## Эпик BA — Admin export raw events §19.20
- `analytics_query.list_raw_events()` + `raw_events_to_csv()`.
- `GET /admin/analytics/events` — фильтры `user_id`, `date_from`, `date_to`, `?format=csv`.

## Эпик BB — Analytics track continue + FAB §19.20
- `pending_upload_continue` — tap «Продолжить» на баннере незавершённой съёмки.
- `shoot_link_fab` — tap FAB shoot-link на home.

## Эпик BC — Campaign CTR §19.20
- Backend: `click_url` в `GET /user/campaign_banners` (`click_track_url`).
- Mobile: `CampaignBanner` CTA + `campaignBannerCta` i18n ×4.
- Track `campaign_banner_click` + `banner_id`; открытие URL через `url_launcher`.

## Эпик BD — Mode switch dedup §19.20
- `_switchMode()`: track `mode_personal` / `mode_corporate` только если режим реально изменился.

### Тесты
- `test_analytics_sync_slice.py`, `test_analytics_query_slice.py`, `test_analytics_events_slice.py` — 12 passed; OpenAPI regen OK.

---

# Итерация 12 — Admin analytics UI + CTR + CH monitoring + track P10 (§19.20)

## Эпик BE — Admin AnalyticsPage §19.20
- `AnalyticsPage.tsx`: screen breakdown, raw events (user_id), CSV export, PG→CH sync.
- Route `/analytics` + nav в web-admin.

## Эпик BF — Campaign banner CTR в админке §19.20
- `analytics_query.campaign_banner_ctr()` — CH/PG, join entitlement→campaign.
- `GET /admin/analytics/campaign-banner-ctr`; `banner_ctr` в `GET /admin/campaigns`.
- CampaignsPage: колонки impressions / clicks / CTR.

## Эпик BG — CH sync monitoring §19.20
- `count_pending()`, `GET /admin/analytics/status` (`pending_ch_sync`, alert >1000).
- Prometheus `kwork_analytics_ch_pending_sync`; Celery warning при backlog.

## Эпик BH — ALLOWED_SCREENS schema §19.20
- `ALLOWED_SCREENS` frozenset + validation unknown screen.
- `campaign_banner_click`, `export_prefs_save`, `queue_refresh` в каталоге.

## Эпик BI — Analytics micro-events §19.20
- `export_prefs_save` при сохранении формата экспорта.
- `queue_refresh` при ручном обновлении очереди.

### Тесты
- `test_analytics_events_slice.py` + `test_analytics_query_slice.py` — 15 passed; OpenAPI regen OK.

---

# Итерация 13 — CH banner MV + sync alerts + charts + track P11 (§19.20)

## Эпик BJ — CH mobile_analytics_banner_daily §19.20
- MV `mobile_analytics_banner_daily` в `infra/clickhouse/init.sql`.
- Backfill: `backfill_mobile_analytics_banner_daily.sql` + `backfill_banner_mv.ps1`.
- `campaign_banner_ctr` читает из MV (не raw events).

## Эпик BK — CH sync Telegram/email alert §19.20
- `analytics_alerts.check_and_alert()` — dual-channel при `pending_ch_sync > 1000`, cooldown 1ч.
- Celery `sync_analytics_to_clickhouse` вызывает alert check.
- `POST /admin/analytics/alerts/check`.

## Эпик BL — Analytics screen timeseries §19.20
- `screen_timeseries()` — CH MV `mobile_analytics_screen_daily` / PG fallback.
- `GET /admin/analytics/screens/timeseries`.
- AnalyticsPage: LineChart top screens по дням (recharts).

## Эпик BM — Analytics micro-events §19.20
- `queue_reconnect_ws` — tap переподключения WS на queue.
- `notifications_mark_read` — open notification + mark all read.
- `ALLOWED_SCREENS` дополнен.

### Тесты
- `test_analytics_alerts_slice.py`, `test_clickhouse_analytics_ddl_slice.py` — 21 passed; OpenAPI regen OK.

---

# Итерация 14 — OPS backfill docs + alert threshold + track/date filter P12 (§19.20)

## Эпик BN — OPS ClickHouse analytics backfill §19.20
- `docs/deployment/OPS_CLICKHOUSE_ANALYTICS.md` — dual backfill `screen` + `banner` MV, env, verify.

## Эпик BO — analytics_ch_sync_pending_max §19.20
- `alert_thresholds`: ключ `analytics_ch_sync_pending_max` (default 1000, env `ANALYTICS_CH_SYNC_PENDING_MAX`).
- `analytics_alerts`, `analytics_sync_status`, Celery — читают порог из thresholds.
- SettingsPage: поле порога.

## Эпик BP — Analytics micro-events §19.20
- `queue_cancel` — подтверждённая отмена заказа в queue.
- `notifications_clear` — очистка inbox.
- `ALLOWED_SCREENS` дополнен.

## Эпик BQ — Admin events date filter §19.20
- AnalyticsPage: `date_from` / `date_to` на load + CSV export raw events.

### Тесты
- `test_alert_settings_slice.py`, `test_analytics_events_slice.py` — slice OK.

---

# Итерация 15 — events pagination + screen filter + track P13 (§19.20)

## Эпик BR — Analytics events pagination §19.20
- `list_raw_events`: `total`, `has_more` в ответе.
- AnalyticsPage: offset/limit, «Назад»/«Вперёд», счётчик диапазона.

## Эпик BS — Analytics screen filter §19.20
- `screen_timeseries(screen=…)` — CH MV / PG single-screen series.
- `GET /admin/analytics/screens/timeseries?screen=`.
- AnalyticsPage: dropdown «Screen (график)».

## Эпик BT — Analytics micro-events §19.20
- `queue_cancel_ack` — подтверждение диалога отмены (processing/generating).
- `support_ticket_open` + `ticket_id` — открытие тикета (list + deep link).
- `ALLOWED_SCREENS` дополнен.

### Тесты
- `test_analytics_events_slice.py`, `test_analytics_query_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 16 — ticket_id validation + support track + event filter P14 (§19.20)

## Эпик BU — Analytics ticket_id validation §19.20
- `support_ticket_open` / `support_ticket_reply`: обязательный `props.ticket_id` int ≥ 1.
- `faq_question_send` в `ALLOWED_SCREENS`.

## Эпик BV — Analytics micro-events §19.20
- Mobile track: `faq_question_send` после успешного ask.
- Mobile track: `support_ticket_reply` + `ticket_id` при ответе в тикете.

## Эпик BW — Admin events event filter §19.20
- `list_raw_events(event=…)` + `GET /admin/analytics/events?event=`.
- AnalyticsPage: dropdown Event type (`screen_view`, `shoot_complete`, …).

### Тесты
- `test_analytics_events_slice.py`, `test_analytics_query_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 17 — screen filter + support/notifications track P15 (§19.20)

## Эпик BX — Analytics screen filter on raw events §19.20
- `list_raw_events(screen=…)` — фильтр по `props.screen` (JSONB).
- `GET /admin/analytics/events?screen=`.
- AnalyticsPage: dropdown Screen (из breakdown).

## Эпик BY — Analytics micro-events §19.20
- `support_ticket_close` + `ticket_id` при закрытии тикета.
- `notifications_mark_all_read` — отдельный event для «прочитать все».

### Тесты
- `test_analytics_events_slice.py`, `test_analytics_query_slice.py` — slice OK.

---

# Итерация 18 — OAuth VK / Yandex / Sber ID (§2.2.3 расширение)

## Эпик BZ — Backend OAuth §2.2.3
- Миграция `036_user_oauth_identities`, `users.password_hash` nullable.
- `GET /auth/oauth/providers`, `GET /auth/oauth/{provider}/authorize`, `POST /auth/oauth/{provider}/callback`.
- Провайдеры: VK ID, Яндекс ID, Сбер ID (`OAUTH_*_CLIENT_ID/SECRET`).

## Эпик CA — Web-seller OAuth §20.1
- `OAuthButtons` на login (`AuthCard`) и register.
- Callback `/auth/oauth/callback` → JWT + redirect dashboard / account-type.

## Эпик CB — Mobile OAuth §16
- API client: authorize URL + callback exchange.
- Кнопки на `AuthScreen`, deep link `kworkmob://open/oauth/callback`.

### Тесты
- `test_oauth_auth_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 19 — OAuth link + pending_type flow P17 (§2.2.3)

## Эпик CC — OAuth link API §2.2.3
- `GET /auth/oauth/identities` — список привязок.
- `GET /auth/oauth/{provider}/link` — authorize для привязки (auth).
- `POST /auth/oauth/{provider}/link` — завершение привязки code+state.

## Эпик CD — Web-seller OAuth link §20.8
- Settings: блок «Вход через соцсети», кнопки «Привязать».
- Callback `oauth_flow=link` → `/settings`.

## Эпик CE — Mobile OAuth polish §16 / §2.2.2
- Splash: `pending_type` → `/auth?mode=account-type`.
- Profile: привязка OAuth (link flow + deep link).

## Эпик CF — OPS OAuth §2.2.3
- `docs/deployment/OPS_OAUTH.md` — env + redirect URIs checklist.

### Тесты
- `test_oauth_auth_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 20 — OAuth unlink + link UX + analytics P18 (§2.2.3 / §19.20)

## Эпик CG — OAuth unlink §2.2.3
- `DELETE /auth/oauth/{provider}/link` — отвязка (защита: нужен пароль или другой OAuth).
- Web-seller settings + mobile profile: кнопка «Отвязать».

## Эпик CH — Mobile OAuth link UX §16
- Deep link link-flow → `/home?tab=profile`.
- `WidgetsBindingObserver`: refresh pending link после возврата из браузера.
- UI: «Привязано» + spinner / кнопки Привязать/Отвязать.

## Эпик CI — OAuth analytics §19.20
- `screen_view`: `oauth_login_{vk|yandex|sber}`, `oauth_link_{…}`.
- `is_oauth_screen()` в schema validation.

### Тесты
- `test_oauth_auth_slice.py`, `test_analytics_events_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 21 — me oauth_providers + analytics oauth filter + web track P19 (§2.2.3 / §19.20)

## Эпик CJ — oauth_providers в /user/me §2.2.3
- `GET /user/me` → `oauth_providers: ["vk", …]` из `user_oauth_identities`.

## Эпик CK — Analytics oauth screens §19.20
- `screen_category=oauth|app` в `/admin/analytics/screens` и `/events`.
- AnalyticsPage: фильтр категории, метрики `oauth_login` / `oauth_link`.

## Эпик CL — Web OAuth server track §19.20
- `record_screen_event` при OAuth callback/link (platform=web).
- `platform` в OAuth state (Redis).

### Тесты
- `test_analytics_query_slice.py`, `test_oauth_auth_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 22 — OAuth audit_log + me.oauth_providers в UI P20 (§2.2.3)

## Эпик CM — OAuth audit_log §2.2.3
- `audit_log`: `oauth_login`, `oauth_link`, `oauth_unlink` + `details.provider` (+ `platform`).

## Эпик CN — Web-seller oauth_providers §20.8
- Settings: привязки из `GET /user/me` (`oauth_providers`), без `fetchOAuthIdentities`.

## Эпик CO — Mobile profile oauth_providers §16
- `AppSession.oauthProviders` из `applyMe`.
- Profile: бейджи «Привязано» по `session.oauthProviders`.

### Тесты
- `test_oauth_auth_slice.py` — audit oauth_login/unlink; slice OK.

---

# Итерация 23 — Admin oauth audit + OAuth me refresh P21 (§2.2.3)

## Эпик CP — Admin audit oauth §2.2.3
- `GET /admin/audit` — фильтр `action` / `action_prefix=oauth_`.
- `GET /admin/audit/oauth-summary` — метрики oauth_login/link/unlink.
- AnalyticsPage: audit-метрики + таблица oauth audit_log.

## Эпик CQ — Web-seller OAuth link refresh §20.8
- Callback link → `sessionStorage.oauth_link_me` → settings без повторного `/user/me`.

## Эпик CR — Mobile OAuth login applyMe §16
- `auth_screen`: `applyMe` после OAuth login/link callback.

### Тесты
- `test_audit_query_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 24 — Company oauth audit + splash link refresh P22 (§2.5.5 / §20.5 / §16)

## Эпик CS — Company audit oauth §2.5.5
- `GET /company/audit`: `action` / `action_prefix=oauth_` (oauth события сотрудников).
- Export CSV с теми же фильтрами.

## Эпик CT — Web-seller team audit filter §20.5
- `/team/audit`: Select oauth_* + колонка provider/platform.

## Эпик CU — Mobile splash OAuth link §16
- Splash: завершение pending OAuth link + `applyMe` до перехода на profile.

### Тесты
- `test_audit_query_slice.py` — company oauth filter; OpenAPI regen.

---

# Итерация 25 — OAuth audit company_id + settings hint + unlink refresh P23 (§2.2.3 / §20.8 / §16)

## Эпик CV — OAuth audit company_id §2.2.3
- `company_id` в audit_log при oauth_* (из OAuth state / query, валидация membership).
- `GET /auth/oauth/*?company_id=` — mobile корп. режим.

## Эпик CW — Settings oauth_unlink hint §20.8
- Последняя `oauth_unlink` из `/company/audit` на settings (Owner).

## Эпик CX — Mobile unlink applyMe §16
- После unlink: только `applyMe`, без `_loadOAuth`; `company_id` в link/unlink API.

### Тесты
- `test_oauth_auth_slice.py`, `test_audit_query_slice.py` — slice OK; OpenAPI regen.

---

# Итерация 26 — User oauth audit + settings personal hint + mobile login company_id P24 (§2.5.5 / §20.8 / §16)

## Эпик CY — User audit oauth §2.5.5
- `GET /user/audit` — `action` / `action_prefix=oauth_` для текущего пользователя.

## Эпик CZ — Settings oauth_unlink personal §20.8
- Owner: `/company/audit`; остальные: `/user/audit` для hint последней отвязки.

## Эпик DA — Mobile OAuth login company_id §16
- `oauthAuthorizeUrl(companyId)` при корп. режиме в `auth_screen`.

## Эпик DB — Cursor agents (workflow)
- `.cursor/skills/kwork-task-planner` — планирование P{N}
- `.cursor/skills/kwork-task-coder` — реализация срезов
- `.cursor/skills/kwork-task-verifier` — проверка готовности задач

### Тесты
- `test_audit_query_slice.py` — user oauth filter; OpenAPI regen.

---

# Итерация 27 — User audit export + OAuth company_id web + profile hint P25 (§2.5.5 / §20.8 / §16)

## Эпик DC — User audit export §2.5.5
- `GET /user/audit/export` — CSV личного audit_log с фильтрами action/action_prefix/days.

## Эпик DD — Web OAuth link company_id §20.8
- `resolveOAuthCompanyId()` из `/company/mine` (корп. режим).
- `startOAuthLink` / `unlinkOAuth` передают `company_id` в API.

## Эпик DE — Mobile profile oauth_unlink hint §16
- `/user/audit?action=oauth_unlink&limit=1` при загрузке профиля и после отвязки.
- Текст «Последняя отвязка: …» под блоком соцсетей.

### Тесты
- `test_audit_query_slice.py` — user audit export rows; OpenAPI regen.

---

# Итерация 28 — OAuth login company_id web + audit export UI P26 (§2.2.3 / §20.8 / §20.5 / §16)

## Эпик DF — Web OAuth login company_id §2.2.3
- `startOAuth` передаёт `company_id` из `resolveOAuthCompanyId()` в `OAuthButtons`.

## Эпик DG — Settings user audit export §20.8
- Кнопка Export CSV → `/user/audit/export?action_prefix=oauth_`.

## Эпик DH — Team audit oauth preset §20.5
- Фильтр по умолчанию `oauth_*` на `/team/audit`.

## Эпик DI — Mobile profile oauth_link hint §16
- Hint «Последняя привязка» из `/user/audit` рядом с oauth_unlink.

---

# Итерация 29 — Admin user audit + OAuth hints P27 (§2.2.3 / §20.8 / §16)

## Эпик DJ — Admin user audit §2.2.3
- `GET /admin/users/{user_id}/audit` — audit_log пользователя для support.

## Эпик DK — Web OAuth corporate badge + login hint §20.8
- `OAuthButtons`: бейдж «Корп. режим» при `resolveOAuthCompanyId()`.
- Settings: hint последнего `oauth_login` из `/user/audit`.

## Эпик DL — Mobile oauth audit hints service §16
- `OAuthAuditHints` + поля в `AppSession`; profile hints login/link/unlink.
- Splash: `OAuthAuditHints.refresh` после pending OAuth link.

### Тесты
- `test_audit_query_slice.py` — admin user audit filter; OpenAPI regen.

---

# Итерация 30 — Admin user audit UI + hints refresh P28 (§2.2.3 / §20.8 / §16)

## Эпик DM — Admin user detail oauth audit §2.2.3
- UserDetailPage: таблица oauth_* из `/admin/users/{id}/audit`.

## Эпик DN — Settings oauth hints refresh §20.8
- После unlink OAuth — обновление hint `oauth_login`.

## Эпик DO — Mobile auth corporate badge §16
- Бейдж «Корп. режим» рядом с OAuth-кнопками при `session.corporate`.

---

# Итерация 31 — Admin user audit export P29 (§2.2.3 / §16)

## Эпик DP — Admin user audit export §2.2.3
- `GET /admin/users/{user_id}/audit/export` CSV.
- UserDetailPage: кнопка Export CSV oauth_*.

## Эпик DQ — Mobile auth oauth_login hints §16
- `OAuthAuditHints.refresh` после успешного OAuth login в `auth_screen`.

### Тесты
- OpenAPI regen.

---

# Итерация 32 — OAuth hints polish P30 (§20.8 / §2.2.3 / §16)

## Эпик DR — Settings oauth link callback hints §20.8
- `refreshOAuthHints()` + повтор после `oauth_link_me`.

## Эпик DS — Admin audit user links §2.2.3
- AnalyticsPage oauth audit: user_id → `/users/:id`.

## Эпик DT — Mobile auth link hints §16
- `OAuthAuditHints.refresh` после OAuth link в `auth_screen`.

---

# Итерация 33 — Company access log export P31 (§10.7.2 / §20.5 / §16)

## Эпик DU — Company access log export §10.7.2
- `GET /company/access-log/export` CSV для Owner.

## Эпик DV — Web team access export §20.5
- Кнопка Export CSV на `/team`.

## Эпик DW — Mobile team access log §16
- Owner: список `/company/access-log` в audit tab.

### Тесты
- `test_access_log_slice.py` — to_csv; OpenAPI regen.

---

# Итерация 34 — User access log P32 (§2.5.5 / §20.8 / §16)

## Эпик DX — User access log API §2.5.5
- `GET /user/access-log` — личные скачивания моделей.

## Эпик DY — Web settings access log §20.8
- Таблица последних скачиваний в settings.

## Эпик DZ — Mobile profile access log §16
- Список `/user/access-log` в профиле.

### Тесты
- `test_access_log_slice.py`; OpenAPI regen.

---

# Итерация 35 — User access export + team funnel P33 (§2.5.5 / §20.8 / §16)

## Эпик EA — User access log export §2.5.5
- `GET /user/access-log/export` CSV.

## Эпик EB — Web settings access export §20.8
- Кнопка Export CSV скачиваний.

## Эпик EC — Mobile team publication funnel §16
- Totals воронки `/company/publication-funnel` на team screen (Owner).

### Тесты
- `test_access_log_slice.py` fix; OpenAPI regen.

---

# Итерация 36 — Shoot links stats + team sessions P34 (§3.15.4 / §20.5 / §16)

## Эпик ED — Shoot links stats test §3.15.4
- Slice test `company_stats` conversion_rate.

## Эпик EE — Web team shoot recent §20.5
- Таблица recent shoot links из `/company/shoot_links/stats`.

## Эпик EF — Mobile team shoot + sessions §16
- Shoot stats на team + shoot_link screen; tab «Сессии» с revoke.

---

# Итерация 37 — Balance presets + invitation revoke P35 (§20.3.4 / §2.5.5 / §16)

## Эпик EG — Invitation revoke API §2.5.5
- `DELETE /company/invitations/{id}` для pending.

## Эпик EH — Web balance presets §20.3.4
- Select + save/delete на `/balance`.

## Эпик EI — Mobile team invitations §16
- Список исходящих приглашений + отмена.

---

# Итерация 38 — Team invite revoke + marketplace status P36 (§20.5 / §16)

## Эпик EJ — Web team invite revoke §20.5
- Кнопка «Отменить» для pending приглашений.

## Эпик EK — Mobile marketplace status §16
- Бейдж WB/Ozon credentials на team screen (Owner).

### Тесты
- `test_company_invitation_revoke_slice.py`

---

# Итерация 39 — Trash filter + user devices P37 (§20.4 / §16 / §2.5.5)

## Эпик EL — Trash publish filter §20.4
- `GET /models/trash?publish_filter=`; web корзина: фильтр + empty state + колонка статуса.

## Эпик EM — Mobile trash refresh §16
- Pull-to-refresh на пустой корзине и при ошибке.

## Эпик EN — User devices list §2.5.5
- `GET /user/devices`; web settings + mobile profile список push-устройств.

### Тесты
- `test_user_devices_slice.py`, `test_trash_pagination_slice.py`; OpenAPI regen.
