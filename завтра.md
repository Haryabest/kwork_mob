# web-admin — довести до ТЗ §11 (~72% → ~90%)

## P0 — заглушки (быстро, видимый эффект)

- [ ] **InvitationsPage** (`AdminPages.tsx`) — подключить API: список активных приглашений B2B (email, компания, роль, срок), кнопка «Отозвать»
- [ ] **SupportStatsPage** — подключить API: SLA, время первого ответа, тикеты за период, нагрузка по агентам (убрать hardcode)

## P1 — B2B в admin (§11.6)

- [ ] **CompanyDetailPage** — лимиты: `max_concurrent_orders`, `monthly_spending_limit` per photographer
- [ ] **CompanyDetailPage** — API-ключи компании: список, создать, отозвать, last_used
- [ ] **CompanyDetailPage** — индивидуальные цены/скидки на тарифы (§11.4)
- [ ] **CompanyDetailPage** — логи действий сотрудников (`GET /admin/companies/{id}/logs`)

## P2 — дашборды и метрики (§11.2)

- [ ] Подключить **recharts** (уже в package.json) — графики orders_hourly, GPU load, revenue
- [ ] Метрики сегментации фона: DeepLab/SAM success rate, mean confidence (если API есть — иначе backend)
- [ ] Индикатор очереди: «Redis OK / PostgreSQL актуальна» на dashboard

## P3 — support, push, legal

- [ ] **TicketDetailPage** — боковая панель пользователя (заказы, сумма, NSFW-история)
- [ ] **PushPage** — логи отправок + расписание «отправить позже»
- [ ] **LegalPage** — preview Markdown/HTML, таблица истории версий

## P4 — налоги, import/export (§11.13–11.14)

- [ ] **TaxPage** — кнопки «Счёт PDF» / «Акт PDF» (если API есть)
- [ ] Import CSV: компании B2B, массовое создание промокодов

## P5 — UX / техдолг (§11.15)

- [ ] Тёмная тема (Mantine colorScheme toggle)
- [ ] Виртуализация больших таблиц (users, logs) — react-window или Mantine DataTable
- [ ] Удалить мёртвые stub-страницы из `AdminPages.tsx` (~751–947)
- [ ] Подключить **React Query** для кэша и пагинации

## P6 — вне §11, но в ТЗ

- [ ] Инструмент проверки watermark (§10.5.3) — upload .glb / MinIO key → результат
- [ ] Autoscaling rules UI — create/edit (сейчас только read + текст «создайте через PUT»)

## Проверка после

```bash
cd apps/web-admin && npm run build
# smoke: login → dashboard → companies → invitations → support/stats
```
