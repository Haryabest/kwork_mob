# Задачи на сегодня (2026-07-21)

Handoff: **P49 закрыт** (итерация 50). Следующий батч — **P50 §2 auth (остаток)**.

```bash
git pull origin main
alembic upgrade head   # 039 user marketing profile
```

---

## Батч P50 — §2 Auth (следующий)

| ID | Задача | Статус |
|----|--------|--------|
| **2.2** | DaData/FNS верификация ИНН юрлиц | PARTIAL |
| **2.4** | PII AES-256 + Vault keys | PARTIAL |
| **2.6** | «Remember me» — единый TTL 30 дней везде | PARTIAL |

Перед стартом — сверить статусы в `ЗАДАЧИ_ПО_ТЗ.md`, не дублировать DONE.

---

## Проверка P49 (локально)

```bash
cd backend/orchestrator && py -m pytest tests/test_auth_marketing_slice.py -q
cd apps/web-seller && npm run typecheck
```

- Login с двух браузеров → старая сессия revoked + уведомление
- `/settings` → пол / регион / банк карты
- Prod: задать `JWT_RSA_PRIVATE_KEY` + `JWT_RSA_PUBLIC_KEY` (PEM)

---

## Чеклист

- [x] P49: 2.1, 2.3, 2.5 → DONE
- [x] Итерация 50 в `чтоготовосегодня.md`
- [x] `git push`
