# Задачи на сегодня (2026-07-21)

Handoff: **P50 закрыт** (итерация 51). Следующий батч — **P51 §3 mobile**.

```bash
git pull origin main
alembic upgrade head   # 040 refresh_remember_me
```

---

## Батч P51 — §3 Mobile (следующий)

| ID | Задача | Статус |
|----|--------|--------|
| **3.1** | Resumable multipart upload ZIP (12 фото) | PARTIAL |
| **3.5** | Cancel order: UX «без возврата на processing» | PARTIAL |
| **3.7** | Public share viewer: rate-limit + watermark overlay | PARTIAL |

Перед стартом — сверить статусы в `ЗАДАЧИ_ПО_ТЗ.md`, не дублировать DONE.

---

## Проверка P50 (локально)

```bash
cd backend/orchestrator && py -m pytest tests/test_auth_p50_slice.py -q
cd apps/web-seller && npm run typecheck
```

- `/register/type` → ИНН → автозаполнение DaData, mismatch + confirm
- Login с «Запомнить меня» → refresh сохраняет 30 дней
- `/health` → `pii_encryption.ok`
- Prod: `DADATA_API_KEY`, `PD_ENCRYPTION_KEY` или Vault

---

## Чеклист

- [x] P50: 2.2, 2.4, 2.6 → DONE
- [x] §2 auth полностью закрыт
- [x] Итерация 51 в `чтоготовосегодня.md`
- [x] `git push`
