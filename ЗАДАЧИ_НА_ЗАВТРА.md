# Задачи на сегодня (2026-07-21)

Handoff: **P51 закрыт** (итерация 52). Следующий батч — **P52 §3 mobile**.

```bash
git pull origin main
```

---

## Батч P52 — §3 Mobile (следующий)

| ID | Задача | Статус |
|----|--------|--------|
| **3.2** | Offline mode (очередь действий без сети) | MISSING |
| **3.4** | Team UI — parity с web team | PARTIAL |
| **3.6** | Push → email fallback на клиенте | PARTIAL |

Перед стартом — сверить статусы в `ЗАДАЧИ_ПО_ТЗ.md`, не дублировать DONE.

---

## Проверка P51 (локально)

```bash
cd backend/orchestrator && py -m pytest tests/test_p51_slice.py -q
cd apps/web-seller && npm run typecheck
```

- Mobile upload: ZIP multipart resume → checkout
- Queue cancel processing: ack без возврата → home (не processing)
- `/share/{hash}`: watermark + rate-limit 429

---

## Чеклист

- [x] P51: 3.1, 3.5, 3.7 → DONE
- [x] Итерация 52 в `чтоготовосегодня.md`
- [x] `git push`
