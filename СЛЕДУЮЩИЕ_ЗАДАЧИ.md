# Следующие задачи — только код (продакшен)

## Сделано в срезе P22

- `GET /company/audit` + `action_prefix=oauth_` для Owner §2.5.5
- Web-seller `/team/audit`: фильтр oauth_* + details provider
- Mobile splash: pending OAuth link → `applyMe` до profile §16

## P23 — backend

1. **OAuth audit company_id** — писать `company_id` в audit_log при oauth_* если пользователь в корп. режиме §2.2.3

## P23 — web-seller

2. **Settings oauth unlink audit hint** — показывать последнее oauth_unlink из `/company/audit?action=oauth_unlink` §20.8

## P23 — mobile

3. **Profile OAuth unlink refresh** — после unlink вызывать `applyMe` без отдельного `_loadOAuth` §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
