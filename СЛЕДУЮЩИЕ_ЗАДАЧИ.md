# Следующие задачи — только код (продакшен)

## Сделано в срезе P41

- `DELETE /user/draft-backups/{model_uuid}` §3.3.2
- Web settings + mobile profile: удаление черновика
- Mobile team: webhook deliveries read-only §14.5

## P42 — web-seller

1. **Team webhooks DLQ badge** — счётчик failed/dlq на `/team/webhooks` из dashboard API §20.5

## P42 — mobile

2. **User audit export** — скачивание `/user/audit/export` из профиля §16
3. **Company roles list** — read-only `/company/roles` на team screen §16

## P42 — backend

4. **User audit export slice test** — `test_user_audit_export_slice.py` §2.5.5

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
