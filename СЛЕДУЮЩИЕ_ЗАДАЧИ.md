# Следующие задачи — только код (продакшен)

## Сделано в срезе P11

- CH MV `mobile_analytics_banner_daily` + backfill SQL/ps1 §19.20
- CH sync alert: `analytics_alerts.check_and_alert()` Telegram/email, Celery + `POST /admin/analytics/alerts/check` §19.20
- Mobile track: `queue_reconnect_ws`, `notifications_mark_read` §19.20
- Analytics charts: `GET /admin/analytics/screens/timeseries` + recharts в AnalyticsPage §19.20
- CTR query переведён на `mobile_analytics_banner_daily` MV §19.20

## P1 — backend

1. **Analytics banner_id MV backfill ops** — документировать dual backfill (screen + banner) в OPS §19.20
2. **CH sync alert threshold** — вынести в `alert_thresholds` / admin settings §19.20

## P1 — mobile

3. **Analytics track** — `notifications_clear`, `queue_cancel` §19.20

## P1 — web-admin

4. **Analytics date filter** — date_from/date_to на raw events export UI §19.20

## Ops (ручное)

- CH backfill staging: `backfill_screen_mv.ps1` + `backfill_banner_mv.ps1`
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
