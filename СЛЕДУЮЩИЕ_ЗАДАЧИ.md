# Следующие задачи — только код (продакшен)

## Сделано в срезе P28

- Web-admin: OAuth audit таблица на `/users/:id` §2.2.3
- Web-seller settings: refresh `oauth_login` hint после unlink §20.8
- Mobile auth: бейдж «Корп. режим» при corporate OAuth §16

## P29 — backend

1. **Admin user audit export** — `GET /admin/users/{id}/audit/export` CSV §2.2.3

## P29 — web-admin

2. **User audit export button** — Export CSV на странице пользователя §2.2.3

## P29 — mobile

3. **Auth oauth_login hint** — `OAuthAuditHints.refresh` после OAuth login в `auth_screen` §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
