# Следующие задачи — только код (продакшен)

## Сделано в срезе P19

- `GET /user/me`: `oauth_providers: []` §2.2.3
- AnalyticsPage: `screen_category` oauth/app + oauth_login/oauth_link totals §19.20
- Server-side oauth track для web-seller (`record_screen_event`, platform=web) §19.20

## P20 — backend

1. **OAuth audit_log** — `oauth_login` / `oauth_link` / `oauth_unlink` в `audit_log` §2.2.3

## P20 — web-seller

2. **Settings oauth_providers** — показывать привязки из `/user/me` без отдельного запроса

## P20 — mobile

3. **Profile oauth_providers** — бейджи из `me.oauth_providers` §2.2.3

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
