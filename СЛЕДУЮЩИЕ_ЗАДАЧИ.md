# Следующие задачи — только код (продакшен)

## Сделано в срезе P17

- OAuth link: `GET/POST /auth/oauth/{provider}/link`, `GET /auth/oauth/identities` §2.2.3
- Web-seller settings: привязка VK/Yandex/Sber
- Mobile: `pending_type` → account-type на splash; OAuth link в профиле
- Ops: `docs/deployment/OPS_OAUTH.md`

## P18 — backend

1. **OAuth unlink** — `DELETE /auth/oauth/{provider}/link` §2.2.3

## P18 — mobile

2. **OAuth link UX** — индикатор «привязано» + refresh после deep link без перезахода

## P18 — analytics

3. **Track oauth_login** — `screen_view` `oauth_login_{provider}` / `oauth_link_{provider}` §19.20

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
