# Следующие задачи — только код (продакшен)

## Сделано в срезе P37

- Web models trash: фильтр публикации + empty state §20.4
- Mobile trash: pull-to-refresh на пустом списке §16
- `GET /user/devices` + UI в settings / profile §2.5.5
- `test_user_devices_slice.py`

## P38 — backend

1. **Device revoke by id** — `DELETE /user/devices/{id}` §2.5.5

## P38 — web-seller

2. **Settings devices revoke** — кнопка отвязки push-устройства §20.8

## P38 — mobile

3. **Profile device revoke** — отвязка из списка §16
4. **Trash publish filter** — фильтр published/draft на `/trash` §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
