# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Web-seller models list badges (import_validating / imported / import_failed)
- Import NSFW scan on GLB textures (worker + block_order refund)
- Company orders filter by employee (API + web-seller §3.16.2)
- Ops: e2e_import_acceptance, e2e_import_api.ps1, e2e_cloud_restore.ps1, E2E_SMOKE_CHECKS.md

## P1 — mobile / продукт

1. **Mobile orders tab** — фильтр по сотруднику (Owner/Manager parity с web-seller §3.16.2)
2. **Import NSFW UI** — статус `blocked_nsfw` в списке моделей / заказов

## P1 — backend / seller

3. **Mass import API** — >10 GLB за раз §6.10 / §4.8.9
4. **Company transactions** — фильтр по сотруднику в истории §8

## Ops (ручное на устройстве)

- iOS AR Texture smoke на физическом iPhone (чеклист в `docs/deployment/E2E_SMOKE_CHECKS.md`)
- E2E import API против staging с реальным worker
- Cloud restore после reinstall — mobile manual pass
