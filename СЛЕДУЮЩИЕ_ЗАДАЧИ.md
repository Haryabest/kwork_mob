# Следующие задачи — только код (продакшен)

## Сделано в срезе P5

- ClickHouse DDL `mobile_analytics_events` + MV `mobile_analytics_daily` §19.20
- Team pagination UI: mobile `team_screen` + web-seller `/team` «Загрузить ещё» §20
- Analytics track: `settings`, shoot wizard (`shoot_category/dome/quality/upload`, `order_checkout`) §19.20
- E2E guest shoot: `e2e_guest_shoot_api.ps1` + integration smoke §3.15
- Ops docs: `OPS_DEVICE_QA.md`, обновлён `E2E_SMOKE_CHECKS.md` §19.21

## P1 — backend

1. **Analytics dashboard** — Grafana panel по `mobile_analytics_daily` §19.20
2. **Guest shoot webhook test** — staging webhook receiver для `shoot_link.uploaded` §3.15

## P1 — mobile

3. **Analytics track** — support/faq, model_viewer оставшиеся §19.20
4. **Team load-more i18n** — отдельный ключ вместо `mvLoadMore` §16
5. **Shoot wizard step events** — `shoot_step` при смене ракурса в dome §19.20

## Ops (ручное)

- iOS AR Texture smoke на iPhone (`OPS_DEVICE_QA.md`)
- Firebase prod push E2E (`scripts/push_e2e.py`)
- Real-device QA + store signing (`docs/mobile/RELEASE.md`)
- E2E `e2e_import_api.ps1` на staging
