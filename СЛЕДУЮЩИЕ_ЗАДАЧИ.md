# Следующие задачи — только код (продакшен)

## Сделано в срезе P15

- Analytics raw events: `?screen=` по `props.screen` §19.20
- Mobile track: `support_ticket_close`, `notifications_mark_all_read` §19.20
- AnalyticsPage: dropdown Screen в raw events §19.20

## Сделано в срезе P16 (OAuth)

- Backend: VK / Yandex / Sber ID — `user_oauth_identities`, API `/auth/oauth/*`
- Web-seller: кнопки OAuth на login/register + callback `/auth/oauth/callback`
- Mobile: OAuth кнопки + deep link `kworkmob://open/oauth/callback`

## P17 — backend

1. **OAuth link existing email** — UI «привязать соцсеть» в настройках + `POST /auth/oauth/{provider}/link` §2.2.3

## P17 — mobile

2. **OAuth account type flow** — после OAuth `pending_type` сразу на account-type без лишнего шага §2.2.2

## P17 — ops

3. **OAuth env checklist** — `OAUTH_*_CLIENT_ID/SECRET` в staging/prod + redirect URIs в кабинетах VK/Yandex/Sber

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
