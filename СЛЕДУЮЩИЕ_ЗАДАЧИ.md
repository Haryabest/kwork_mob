# Следующие задачи — только код (продакшен)

## Сделано в срезе P26

- Web `startOAuth` с `company_id` при корп. режиме §2.2.3
- Settings: кнопка Export CSV `/user/audit/export` (oauth_*) §20.8
- Team audit: пресет фильтра `oauth_` по умолчанию §20.5
- Mobile profile: hint последней `oauth_link` из `/user/audit` §16

## P27 — backend

1. **Admin user audit** — `GET /admin/users/{id}/audit` для support §2.2.3

## P27 — web-seller

2. **Login page corporate hint** — бейдж «корп. режим» при OAuth login с company_id §20.8
3. **Settings oauth_login hint** — последний `oauth_login` из `/user/audit` §20.8

## P27 — mobile

4. **Profile oauth_login hint** — текст последнего `oauth_login` §16
5. **Auth screen link hint refresh** — `_loadOAuthAuditHints` после splash link §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
