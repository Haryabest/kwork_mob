# Следующие задачи — только код (продакшен)

## Сделано в срезе P18

- OAuth unlink: `DELETE /auth/oauth/{provider}/link` §2.2.3
- Mobile link UX: deep link → profile, lifecycle refresh, «Привязано»/«Отвязать»
- Analytics: `oauth_login_{provider}` / `oauth_link_{provider}` §19.20

## P19 — backend

1. **OAuth identities in /user/me** — `oauth_providers: []` в профиле §2.2.3

## P19 — web-admin

2. **Analytics oauth screens** — фильтр/группировка oauth_* в AnalyticsPage §19.20

## P19 — mobile

3. **OAuth login web track parity** — track на web-seller login OAuth (если нужен server-side audit)

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
