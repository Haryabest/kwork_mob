# Следующие задачи — только код (продакшен)

## Сделано в срезе P27

- `GET /admin/users/{id}/audit` для support §2.2.3
- Web: бейдж «Корп. режим» на OAuth login + hint `oauth_login` в settings §20.8
- Mobile: `OAuthAuditHints` — login/link/unlink hints; refresh после splash link §16

## P28 — web-admin

1. **User detail audit tab** — таблица oauth_* из `/admin/users/{id}/audit` §2.2.3

## P28 — web-seller

2. **Settings oauth hints refresh** — обновлять login/unlink hints после link/unlink OAuth §20.8

## P28 — mobile

3. **Auth corporate oauth badge** — бейдж «Корп. режим» на экране входа при corporate §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
