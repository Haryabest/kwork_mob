# Следующие задачи — только код (продакшен)

## Сделано в срезе P20

- `audit_log`: `oauth_login` / `oauth_link` / `oauth_unlink` §2.2.3
- Web-seller settings: привязки из `/user/me` (`oauth_providers`)
- Mobile profile: бейджи из `me.oauth_providers` §2.2.3

## P21 — backend

1. **Admin audit oauth** — фильтр `action` oauth_* в `/admin/audit` + метрики на AnalyticsPage §2.2.3

## P21 — web-seller

2. **OAuth callback link refresh** — после link-callback обновлять `me.oauth_providers` без полного reload settings

## P21 — mobile

3. **OAuth login refresh me** — после OAuth login callback вызывать `applyMe` для `oauth_providers` §2.2.3

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
