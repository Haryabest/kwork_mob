# Следующие задачи — только код (продакшен)

## Сделано в срезе P1+P2

- Models server-side filters + pagination `GET /user/models` §19.9
- Analytics events validation `POST /user/analytics/events` §19.20
- Analytics track: calibration/storage/import/trash/queue §19.20
- Model viewer API upload i18n §16
- Balance re-sync on corp/personal switch §20.3.4

## P1 — backend

1. **Web-seller `/models` filters parity** — те же query params + пагинация на seller API §20
2. **OpenAPI regen** — `scripts/export_openapi.py --check` после новых query params §15

## P1 — mobile

3. **Analytics track** — notifications/team/api-keys/publish-guide screens §19.20
4. **Profile/security i18n kk/zh** — password/2FA/delete оставшиеся строки §19.14.4
5. **Models trash server pagination** — `GET /models/trash` offset/limit §3.3.1

## Ops (ручное)

- iOS AR Texture smoke на iPhone
- E2E `e2e_import_api.ps1` на staging
- Firebase prod push E2E
- Grafana env на стенде
- Real-device QA + store signing
