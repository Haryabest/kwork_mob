# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Mass import UI web-seller (11–100 GLB)
- Mobile company balance + фильтр транзакций §8
- Queue detail blocked_nsfw banner (mobile + seller)
- Mass import via X-API-Key scope import:create

## P1 — backend / seller

1. **Export transactions CSV** — company scope с user_id filter на backend
2. **Company balance topup** — web-seller пополнение корпоративного счёта (сейчас только личный / team policies)

## P1 — mobile

3. **Push prefs** — отдельный toggle для NSFW / refund уведомлений
4. **Bulk import** — только API/ERP (UI не нужен на mobile по ТЗ)

## Ops (ручное)

- iOS AR Texture smoke на iPhone
- E2E `e2e_import_api.ps1` на staging с X-API-Key
- Cloud restore после reinstall — mobile manual
