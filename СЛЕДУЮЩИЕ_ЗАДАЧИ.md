# Следующие задачи — только код (продакшен)

## Сделано в срезе P24

- `GET /user/audit` + `action_prefix=oauth_` §2.5.5
- Web-seller settings: hint oauth_unlink из `/user/audit` (не-Owner) / `/company/audit` (Owner)
- Mobile: `company_id` в OAuth authorize при корп. режиме §16
- Agents: `.cursor/skills/kwork-task-{planner,coder,verifier}`

## P25 — backend

1. **User audit export** — `GET /user/audit/export` CSV §2.5.5

## P25 — web-seller

2. **OAuth link company_id web** — `startOAuthLink` / `unlinkOAuth` с `company_id` при корп. режиме §20.8

## P25 — mobile

3. **Profile oauth unlink hint** — snackbar/текст последней личной `oauth_unlink` из `/user/audit` §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
