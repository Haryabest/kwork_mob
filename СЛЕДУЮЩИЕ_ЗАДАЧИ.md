# Следующие задачи — только код (продакшен)

## Сделано в срезе P25

- `GET /user/audit/export` CSV §2.5.5
- Web-seller: `startOAuthLink` / `unlinkOAuth` с `company_id` при корп. режиме §20.8
- Mobile profile: hint последней личной `oauth_unlink` из `/user/audit` §16

## P26 — backend

1. **OAuth login company_id web** — `startOAuth` / authorize с `company_id` при корп. режиме §2.2.3

## P26 — web-seller

2. **Settings user audit export** — кнопка скачать CSV `/user/audit/export` §20.8
3. **Team audit oauth export** — пресет `action_prefix=oauth_` на `/team/audit` §20.5

## P26 — mobile

4. **Profile oauth link hint** — текст последней `oauth_link` из `/user/audit` §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
