# Следующие задачи — только код (продакшен)

## Сделано в срезе P10

- web-admin `AnalyticsPage` — screens breakdown, raw events export, PG→CH sync §19.20
- CampaignsPage — колонки banner impressions/clicks/CTR (CH/PG) §19.20
- CH sync monitoring — `pending_ch_sync`, Prometheus gauge, alert >1000 §19.20
- `ALLOWED_SCREENS` + validation `campaign_banner_click` в analytics schema §19.20
- Mobile track: `export_prefs_save`, `queue_refresh` §19.20

## P1 — backend

1. **Analytics banner_id MV** — CH MV `mobile_analytics_banner_daily` для CTR без PG join §19.20
2. **CH sync alert** — Telegram/email при `pending_ch_sync > threshold` §19.20

## P1 — mobile

3. **Analytics track** — micro-events: `queue_reconnect_ws`, `notifications_mark_read` §19.20

## P1 — web-admin

4. **Analytics charts** — график screen views по дням (recharts) §19.20

## Ops (ручное)

- CH backfill на staging: `infra/clickhouse/backfill_screen_mv.ps1`
- iOS AR / Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit checklist (`docs/mobile/RELEASE.md`)
