# Следующие задачи — только код (продакшен)

## Сделано в срезе P21

- `GET /admin/audit` + `action_prefix=oauth_` + `/admin/audit/oauth-summary` §2.2.3
- AnalyticsPage: audit oauth_login/link/unlink + таблица audit_log
- Web-seller: link-callback → `oauth_link_me` cache → settings без лишнего `/user/me`
- Mobile: `applyMe` после OAuth login/link в `auth_screen` §2.2.3

## P22 — backend

1. **Company audit oauth** — `GET /company/audit?action_prefix=oauth_` для Owner §2.5.5

## P22 — web-seller

2. **Team audit oauth filter** — фильтр oauth_* на `/team/audit` §20.5

## P22 — mobile

3. **Splash OAuth me** — при cold start после OAuth link deep link обновлять `oauth_providers` до открытия profile §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
