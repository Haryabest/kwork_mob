# Следующие задачи — только код (продакшен)

## Сделано в срезе P6

- Grafana panels `mobile_analytics_daily` в `kwork-clickhouse.json` §19.20
- Guest shoot webhook: `test_guest_shoot_webhook_smoke.py` + `e2e_guest_shoot_webhook.ps1` §3.15
- Analytics track: `faq_support`, `models`, `model_viewer` §19.20
- Team i18n: `teamLoadMore` ×4 языка §16
- `shoot_step` event + track в `guided_dome_screen` §19.20

## P1 — backend

1. **Grafana screen breakdown** — MV по `props.screen` из `mobile_analytics_events` §19.20
2. **Webhook DLQ replay test** — integration smoke для replay-dlq §14.5.4

## P1 — mobile

3. **Analytics track** — campaign banners, export prefs §19.20
4. **shoot_step_retry** — при пересъёмке кадра §19.20
5. **Models list dedup** — не дублировать `screen_view models` с home tab §19.20

## Ops (ручное)

- iOS AR Texture smoke на iPhone (`OPS_DEVICE_QA.md`)
- Firebase prod push E2E (`scripts/push_e2e.py`)
- Real-device QA + store signing (`docs/mobile/RELEASE.md`)
- Staging: `e2e_guest_shoot_webhook.ps1` с `WEBHOOK_URL`
