# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Node availability timeline §11.16.3 (`029`, Celery sample, StoragePage)
- B2B webhook delivery retries dashboard (admin + seller)
- «Продлить хранение» исходников ×3 §9.1.2 (API + seller + mobile)
- Disk fill forecast / wearout alerts §23.7 (samples + storage_alerts)
- Trash / restore исходников из облака (seller `/models/trash` + mobile)

## P1 — продукт (код)

1. **Cloud burn** hard-stop polish (лимит облачного хранения)
2. **Owner mass extend** storage для всех моделей компании §9.1.2
3. **Mobile trash list** — экран корзины + restore-from-trash
4. **Webhook delivery** detail modal + manual retry из admin dashboard
5. **Node timeline** — экспорт CSV / zoom по узлу

## Ops (не код)

- Alembic → `029` на стенде
- Celery beat: `storage-health-sample` (*/5) + `model-trash-purge-daily`
- Wire `SOURCE_PHOTOS_TTL_DAYS`, MINIO_HA_JSON nodes для timeline
- Prod: Loki / SMART agents на узлах
