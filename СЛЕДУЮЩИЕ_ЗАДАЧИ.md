# Следующие задачи — только код (продакшен)

## Сделано в срезе P14

- Analytics ticket_id validation: `support_ticket_open`/`support_ticket_reply` §19.20
- Mobile track: `support_ticket_reply`, `faq_question_send` §19.20
- Admin raw events: `?event=` filter + dropdown в AnalyticsPage §19.20

## P15 — backend

1. **Analytics screen filter on raw events** — `?screen=` по `props.screen` в `list_raw_events` §19.20

## P15 — mobile

2. **Analytics track** — `support_ticket_close`, `notifications_mark_all_read` §19.20

## P15 — web-admin

3. **Analytics events screen filter** — dropdown по screen в raw events (синхрон с backend) §19.20

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
