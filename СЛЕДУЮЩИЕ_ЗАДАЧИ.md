# Следующие задачи — только код (продакшен)

## Сделано в срезе P23

- OAuth audit_log: `company_id` при корп. режиме (`company_id` в state/query) §2.2.3
- Web-seller settings: hint последней `oauth_unlink` из `/company/audit`
- Mobile: unlink → `applyMe` без `_loadOAuth`; `company_id` в link/unlink

## P24 — backend

1. **User oauth audit** — `GET /user/audit?action_prefix=oauth_` для личного кабинета §2.5.5

## P24 — web-seller

2. **Settings oauth_unlink personal** — hint из `/user/audit` для не-Owner §20.8

## P24 — mobile

3. **OAuth login company_id** — передавать `company_id` в authorize при корп. режиме §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
