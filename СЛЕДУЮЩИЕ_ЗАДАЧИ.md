# Следующие задачи — только код (продакшен)

## Сделано в срезе P29

- `GET /admin/users/{id}/audit/export` CSV §2.2.3
- Web-admin: Export CSV на странице пользователя §2.2.3
- Mobile auth: `OAuthAuditHints.refresh` после OAuth login §16

## P30 — web-seller

1. **Settings link callback hints** — refresh oauth_login/unlink после `oauth_link_me` §20.8

## P30 — web-admin

2. **Admin audit user link** — user_id в `/audit` → ссылка на `/users/:id` §2.2.3

## P30 — mobile

3. **Auth link oauth hints** — `OAuthAuditHints.refresh` после OAuth link в auth_screen §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
