# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Worker thumbnail.jpg 512×512 при генерации
- Import pricing — тариф import_glb (admin settings + billing)
- Models list — badge import_validating / imported (mobile)

## P1 — mobile / продукт

1. **Seller models list** — badge import_validating / imported (web-seller parity)
2. **Import refund** — возврат на баланс при import_failed §6.10

## P1 — backend / seller

3. **Import NSFW scan** on uploaded GLB (optional)
4. **Company orders tab** — фильтр по сотруднику (Manager)

## Ops

- E2E import GLB → worker validate → completed + thumbnail
- iOS AR Texture smoke на устройстве
- E2E cloud restore после reinstall
