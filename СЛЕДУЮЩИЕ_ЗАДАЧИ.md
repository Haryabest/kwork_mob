# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Export company transactions CSV (backend + user_id filter)
- Web-seller company balance topup (Owner)
- Mobile push prefs: NSFW + refund toggles

## P1 — seller / backend

1. **Balance date filters** — web-seller UI для `from`/`to`/`type` на экспорте и списке
2. **Company low balance notify** — настройка порога в web-seller §20.3.5
3. **SBP QR topup** — QR-код СБП в модалке пополнения §20.3.3

## P1 — mobile

4. **Push master toggles** — push_enabled / email_enabled в профиле §19.14.3
5. **Notifications inbox** — экран колокольчика §19.16 с типами NSFW/refund

## Ops (ручное)

- iOS AR Texture smoke на iPhone
- E2E `e2e_import_api.ps1` на staging с X-API-Key
- Cloud restore после reinstall — mobile manual
