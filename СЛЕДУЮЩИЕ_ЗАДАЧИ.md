# Следующие задачи — только код (продакшен)

## Сделано в срезе P30

- Web-seller settings: `refreshOAuthHints` после `oauth_link_me` callback §20.8
- Web-admin Analytics: user_id в oauth audit → ссылка `/users/:id` §2.2.3
- Mobile auth: `OAuthAuditHints.refresh` после OAuth link §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
