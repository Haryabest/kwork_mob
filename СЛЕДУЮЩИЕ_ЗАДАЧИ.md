# Следующие задачи — только код (продакшен)

## Сделано в срезе P9

- Analytics PG→CH sync: `ch_synced_at`, `analytics_sync.sync_unsynced()`, Celery beat 15 мин, `POST /admin/analytics/sync` §19.20
- Admin `GET /admin/analytics/events` (+ CSV) — raw events по user_id/date §19.20
- Analytics track: `pending_upload_continue`, `shoot_link_fab` §19.20
- Campaign CTR: CTA в `CampaignBanner`, `click_url` из API, track `campaign_banner_click` + `banner_id` §19.20
- Mode switch dedup — track только при реальной смене режима §19.20

## P1 — web-admin

1. **Analytics dashboard page** — UI для `/admin/analytics/screens` + export events §19.20
2. **Campaign CTR в админке** — колонка clicks/impressions в CampaignsPage (CH MV) §19.20

## P1 — backend

3. **CH sync monitoring** — метрика/алерт `pending_ch_sync` (rows где `ch_synced_at IS NULL`) §19.20
4. **campaign_banner_click** — документация в analytics schema / ALLOWED screens list §19.20

## P1 — mobile

5. **Analytics track** — оставшиеся micro-events (export prefs save, queue refresh) §19.20

## Ops (ручное)

- CH backfill на staging: `infra/clickhouse/backfill_screen_mv.ps1`
- iOS AR / Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit checklist (`docs/mobile/RELEASE.md`)
