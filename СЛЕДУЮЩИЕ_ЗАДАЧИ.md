# Следующие задачи — только код (продакшен)

## Сделано в срезе P31

- `GET /company/access-log/export` CSV §10.7.2
- Web-seller team: Export CSV access log §20.5
- Mobile team: access log Owner в audit tab §16

## P32 — backend

1. **User access log** — `GET /user/access-log` личные скачивания §2.5.5

## P32 — web-seller

2. **Settings access log** — таблица последних скачиваний из `/user/access-log` §20.8

## P32 — mobile

3. **Profile access log** — список скачиваний из `/user/access-log` §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
