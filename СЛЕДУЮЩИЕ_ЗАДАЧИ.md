# Следующие задачи — только код (продакшен)

## Сделано в срезе P8

- CH backfill: `backfill_mobile_analytics_screen_daily.sql` + `backfill_screen_mv.ps1` §19.20
- Admin `GET /admin/analytics/screens` (+ CSV `?format=csv`) §19.20
- Analytics track: `pending_upload_banner`, `mode_personal`/`mode_corporate` §19.20
- Campaign banner: `banner_id` в props + backend validation §19.20
- `shoot_step_retry` при auto-fail `missing_frame` в quality review §19.20

## P1 — backend

1. **Analytics PG→CH sync job** — периодический backfill из PG в CH §19.20
2. **Admin analytics events** — export raw events по user_id/date §19.20

## P1 — mobile

3. **Analytics track** — pending upload continue tap, shoot-link FAB §19.20
4. **Campaign CTR** — track click на CTA в banner (если появится) §19.20
5. **Mode switch dedup** — не трекать если режим не изменился §19.20

## Ops (ручное)

- CH backfill на staging: `infra/clickhouse/backfill_screen_mv.ps1`
- iOS AR / Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
