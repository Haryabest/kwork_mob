# Следующие задачи — только код (продакшен)

## Сделано в срезе P32

- `GET /user/access-log` личные скачивания §2.5.5
- Web-seller settings: таблица `/user/access-log` §20.8
- Mobile profile: список скачиваний §16

## P33 — backend

1. **User access log export** — `GET /user/access-log/export` CSV §2.5.5

## P33 — web-seller

2. **Settings access export** — кнопка Export CSV `/user/access-log/export` §20.8

## P33 — mobile

3. **Team publication funnel** — блок stats из `/company/publication-funnel` на team screen §16

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
