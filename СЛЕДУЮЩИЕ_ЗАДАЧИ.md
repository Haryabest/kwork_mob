# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Balance pagination 20/50/100 (backend + web-seller)
- SBP payment status poll (web + mobile)
- Marketing opt-in §20.8.3 (web-seller settings)
- Notifications server inbox API + mobile sync
- Mobile balance filters from/to/type + pagination

## P1 — seller / backend

1. **Transaction status column** — UI колонка статуса транзакции §20.3.4
2. **Company topup mobile** — Owner пополнение корпоративного счёта на mobile §19.14.2
3. **Notifications web** — inbox колокольчик в web-seller §20.x

## P1 — mobile

4. **Date picker mobile** — native date picker вместо текстового поля
5. **Push deeplink inbox** — открытие из push сразу в нужный экран

## Ops (ручное)

- iOS AR Texture smoke на iPhone
- E2E `e2e_import_api.ps1` на staging с X-API-Key
- Cloud restore после reinstall — mobile manual
