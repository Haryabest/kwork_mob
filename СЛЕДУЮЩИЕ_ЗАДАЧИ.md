# Следующие задачи — только код (продакшен)

## Сделано в срезе P7

- ClickHouse MV `mobile_analytics_screen_daily` + Grafana panel screen_view breakdown §19.20
- Webhook DLQ replay: `test_webhook_replay_dlq_smoke.py` §14.5.4
- Analytics track: `campaign_banner`, `campaign_banner_dismiss`, `export_prefs` §19.20
- `shoot_step_retry` event + track в `quality_review_screen` §19.20
- Убран дубль `screen_view models` из `ModelsScreen` (home tab) §19.20

## P1 — backend

1. **CH screen MV backfill** — миграция/скрипт для существующих CH инстансов §19.20
2. **Analytics export API** — admin query screen breakdown из CH §19.20

## P1 — mobile

3. **Analytics track** — pending upload banner, mode switch §19.20
4. **Campaign banner** — track `banner_id` в props (backend whitelist) §19.20
5. **Quality review** — `shoot_step_retry` при auto-fail (missing frame) §19.20

## Ops (ручное)

- iOS AR Texture smoke на iPhone (`OPS_DEVICE_QA.md`)
- Firebase prod push E2E (`scripts/push_e2e.py`)
- Real-device QA + store signing (`docs/mobile/RELEASE.md`)
- Staging: `e2e_guest_shoot_webhook.ps1` с `WEBHOOK_URL`
