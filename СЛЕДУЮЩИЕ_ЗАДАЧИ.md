# Следующие задачи — только код (продакшен)

## Сделано в этом срезе

- Mobile orders — фильтр по сотруднику §3.16.2
- Import NSFW UI — blocked_nsfw в списках моделей/заказов
- Mass import API — bulk до 100 GLB §6.10
- Company transactions — фильтр по сотруднику §8

## P1 — mobile / seller

1. **Mass import UI** — web-seller/admin загрузка >10 GLB (сейчас только API)
2. **Mobile company balance** — экран транзакций компании с фильтром §8
3. **Queue detail** — blocked_nsfw banner на экране заказа (mobile + seller)

## P1 — backend

4. **Mass import API key** — bulk import через X-API-Key (как orders/bulk)
5. **Export transactions CSV** — company scope с user_id filter на backend

## Ops (ручное)

- iOS AR Texture smoke на iPhone (`docs/deployment/E2E_SMOKE_CHECKS.md`)
- E2E `e2e_import_api.ps1` на staging
- Cloud restore после reinstall — mobile manual
