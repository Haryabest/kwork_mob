# Следующие задачи — только код (продакшен)

## Сделано в срезе P12

- OPS: `docs/deployment/OPS_CLICKHOUSE_ANALYTICS.md` — dual backfill screen + banner §19.20
- `analytics_ch_sync_pending_max` в `alert_thresholds` + Settings UI §19.20
- Mobile track: `queue_cancel`, `notifications_clear` §19.20
- AnalyticsPage: date_from/date_to на raw events + CSV export §19.20

## P1 — backend

1. **Analytics events pagination** — cursor/offset UI в admin для больших выборок §19.20

## P1 — mobile

2. **Analytics track** — `queue_cancel_ack` (confirm dialog), `support_ticket_open` §19.20

## P1 — web-admin

3. **Analytics screen filter** — dropdown top screen в timeseries chart §19.20

## Ops (ручное)

- CH backfill staging: `backfill_screen_mv.ps1` + `backfill_banner_mv.ps1` (`OPS_CLICKHOUSE_ANALYTICS.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
