# Следующие задачи — только код (продакшен)

## Сделано в срезе P42

- Web `/team/webhooks`: DLQ + Failed badges из dashboard §20.5
- Mobile profile: экспорт OAuth audit CSV §16
- Mobile team: read-only company roles §16
- `test_user_audit_export_slice.py`

## P43 — mobile

1. **User access-log export** — CSV `/user/access-log/export` из профиля §16
2. **Company access-log export** — CSV на team audit tab (Owner) §16

## P43 — web-seller

3. **Team page webhook DLQ hint** — badge DLQ count в SubNav Webhooks §20.5

## P43 — backend

4. **Access log export slice** — `test_user_access_log_export_slice.py` §2.5.5

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- OAuth prod keys (`OPS_OAUTH.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
