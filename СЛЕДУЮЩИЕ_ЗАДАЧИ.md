# Следующие задачи — только код (продакшен)

Тесты/прогоны на GPU и устройстве — дома. Здесь только прод-код по ТЗ.

---

## P1 — soft launch / ops

1. **Admin Logs** — реальная страница: Loki / ClickHouse / proxy `docker logs` (не placeholder).  
2. **HAProxy + Patroni cutover** — `infra/ha/haproxy.cfg` + compose + runbook `docs/deployment/HA.md`.  
3. **Grafana datasources** — Prometheus + ClickHouse auto-provision.  
4. **MinIO node agent (SMART)** — exporter SMART/disk % → Prometheus (сейчас S3 usage).  
5. **Авто-блокировка неактивных** — Celery по `auto_block_inactive_days` из политик.

---

## P2 — продукт / compliance

6. **Vault/AES для ПД** (§2.7) — ФИО/реквизиты at rest.  
7. **E2E-шифрование фото** (§10.6.2) — company flag + client encrypt.  
8. **WB/Ozon seller API** — загрузка модели через API маркетплейса.  
9. **Воронка публикации** — оплата→генерация→скачивание→verify в dashboard.  
10. **Температура телефона §3.8.2** — battery thermal → FPS 15 / предупреждение >45°C.  
11. **Seller support UI** — FAQ + обращения в кабинете селлера (§20.7).

---

## Сделано в этом срезе

- Политики §2.5.4: structured API (`/company/settings|policies`) + seller форма  
- Load harness: `scripts/load/` (k6 + locust)  
- Авто-refund в `mark_failed` (карта / company / личный баланс)  
- Mobile FAQ/support → `/faq`, `/support`  
- Ollama: `POST /admin/support/.../ai-suggest` + кнопка в admin  
- Device benchmark §3.8 → `ResolutionPreset`  
- AR bbox → `scale_calibration` + подсказка тарифа  

## Не в списке (дома)

- GPU E2E, Firebase push, alembic upgrade, прогон k6/locust  

Источник: `ТЗ.txt` §2.5.4 / §3.8 / §3.13 / §6.12 / §11 / §14 / §20–23.
