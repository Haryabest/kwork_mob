# Следующие задачи — только код (продакшен)

## Сделано в срезе P13

- Analytics events pagination: `total`/`has_more` + offset UI в AnalyticsPage §19.20
- Mobile track: `queue_cancel_ack`, `support_ticket_open` (+ `ticket_id`) §19.20
- Analytics screen filter: `?screen=` на timeseries + dropdown в chart §19.20

## P1 — backend

1. **Analytics ticket_id validation** — optional `props.ticket_id` для `support_ticket_open` §19.20

## P1 — mobile

2. **Analytics track** — `support_ticket_reply`, `faq_question_send` §19.20

## P1 — web-admin

3. **Analytics events event filter** — dropdown по event type в raw events §19.20

## Ops (ручное)

- CH backfill staging (`OPS_CLICKHOUSE_ANALYTICS.md`)
- Firebase prod / real-device QA (`OPS_DEVICE_QA.md`)
- RuStore submit (`docs/mobile/RELEASE.md`)
