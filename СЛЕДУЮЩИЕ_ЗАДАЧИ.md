# Следующие задачи — только код (продакшен)

## Сделано в срезе P1 (mobile)

- Models filters UI (search/date/tier/author) §19.9
- Analytics `track()` screen_view / shoot_complete / checkout_pay §19.20
- Checkout offline `errorNetwork` §19.8
- Balance named presets API + mobile UI §20.3.4
- Profile sessions list/revoke mobile §19.14.4

## P1 — backend

1. **Models server-side filters** — query params на `GET /user/models` (date/tier/author/search) + пагинация §19.9
2. **Analytics events schema** — валидация payload на `POST /user/analytics/events` §19.20

## P1 — mobile

3. **Analytics full coverage** — track на calibration/storage/import/trash/queue §19.20
4. **Model viewer API upload i18n** — оставшиеся `const Text` в upload-диалогах §16
5. **Balance re-sync on corp switch** — `didUpdateWidget` на `BalanceScreen` при смене режима §20.3.4

## Ops (ручное)

- iOS AR Texture smoke на iPhone
- E2E `e2e_import_api.ps1` на staging с X-API-Key
- Firebase prod push E2E
- Grafana: `GRAFANA_TELEGRAM_*` / `GRAFANA_SLACK_WEBHOOK_URL` на стенде
- Real-device QA + store signing
